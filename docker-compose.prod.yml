services:
  backend:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
      target: prod
    env_file:
      - .env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_FORMAT: json
      POSTGRES_HOST: postgres
      MINIO_ENDPOINT: minio:9000
      CORS_ORIGINS: ${CORS_ORIGINS:-http://127.0.0.1:8080,http://localhost:8080}
      REDIS_URL: redis://redis:6379/0
      TRUSTED_PROXY_CIDRS: ${TRUSTED_PROXY_CIDRS:-172.30.0.0/24}
      METRICS_ENABLED: "true"
      READINESS_PATH: /ready
      OTEL_ENABLED: "true"
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318/v1/traces
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - |
          import urllib.request
          urllib.request.urlopen("http://127.0.0.1:8000/health", timeout=5)
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  frontend:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
      target: prod
      args:
        VITE_API_BASE_URL: /api/v1
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - /dev/null
        - http://127.0.0.1/
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  reverse-proxy:
    image: nginx:1.28-alpine
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    ports:
      - "${PROXY_HTTP_PORT:-80}:80"
      - "${PROXY_HTTPS_PORT:-443}:443"
    volumes:
      - ./docker/nginx/${NGINX_CONF_FILE:-prod.conf}:/etc/nginx/conf.d/default.conf:ro
      - certbot_www:/var/www/certbot:ro
      - certbot_conf:/etc/letsencrypt:ro
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - /dev/null
        - http://127.0.0.1/health
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt
    entrypoint:
      - sh
      - -c
      - 'trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot --quiet; sleep 12h & wait $${!}; done'

  postgres:
    image: postgres:16-bookworm
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-app}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD phải được đặt trong .env, không dùng giá trị mặc định của image}
    ports:
      # CHỈ bind vào 127.0.0.1 của VPS — không phải "0.0.0.0:5432:5432".
      # Không public port này ra internet; chỉ dùng để SSH tunnel từ xa (DBeaver...)
      # kết nối vào cổng loopback của chính VPS. Xem
      # "11. Kết nối DB từ xa bằng DBeaver qua SSH tunnel" trong
      # docs/runbooks/deploy-vps-production.md.
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-app}
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:?MINIO_ACCESS_KEY phải được đặt trong .env, không dùng giá trị mặc định của image}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:?MINIO_SECRET_KEY phải được đặt trong .env, không dùng giá trị mặc định của image}
    ports:
      # Chỉ bind vào 127.0.0.1 của VPS, giống Postgres — dùng để SSH tunnel
      # xem MinIO console qua trình duyệt, không public ra internet. Xem
      # "11. Kết nối DB từ xa bằng DBeaver qua SSH tunnel" trong
      # docs/runbooks/deploy-vps-production.md.
      - "127.0.0.1:9001:9001"
    healthcheck:
      test:
        - CMD
        - mc
        - ready
        - local
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    volumes:
      - minio_prod_data:/data

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_prod_data:/data

  worker:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
      target: prod
    command: ["arq", "app.worker.WorkerSettings"]
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      LOG_FORMAT: json
      POSTGRES_HOST: postgres
      MINIO_ENDPOINT: minio:9000
      REDIS_URL: redis://redis:6379/0
      OTEL_ENABLED: "true"
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318/v1/traces

volumes:
  postgres_prod_data:
  minio_prod_data:
  redis_prod_data:
  certbot_www:
  certbot_conf:

networks:
  default:
    ipam:
      config:
        - subnet: ${PROD_DOCKER_SUBNET:-172.30.0.0/24}
