# Progress

## Completed

- Installed local skills into `.agents/skills/`
- Added root `AGENTS.md`
- Mirrored `axiomhq/agent-memory` into `vendor/agent-memory/`
- Added project-local `memory-bank/`
- Added project-local `.agent-memory/` layout
- Added docs for memory integration and agent rules
- Added fullstack boilerplate design doc in `docs/fullstack-boilerplate-design.md`
- Added requirements for Docker dev/prod/test and automatic test frameworks for backend/frontend
- Added system-wide dark/light theme consistency requirement for buttons, menus, headers, data tables, form inputs, and shared UI components
- Added a mandatory mobile-responsive requirement for frontend layout and shared UI
- Upgraded design requirements to enterprise level: User import/export, large DataTable performance, heavy file import/export jobs, and stronger security baseline
- Added production readiness requirements: observability, backup/restore, secret management, SLO, and compliance gates
- Added Phase 1 scaffold implementation plan in `docs/phase-1-scaffold-implementation-plan.md`
- Completed Phase 1 Step 1 repository preparation: `.gitignore`, `README.md`, `.env.example`, and vendor Git isolation
- Completed Phase 1 Step 2 backend scaffold: `backend/` app package, FastAPI `0.136.3`, typed settings, request-id middleware, SQLAlchemy async session skeleton, MinIO client scaffold, Alembic stub, `uv.lock`, and verified `pytest` + `ruff` + `mypy`
- Completed Phase 1 Step 3 frontend scaffold: `frontend/` app package, Vue `3.5.x`, Vite `8`, Router, Pinia, PrimeVue `4.5.5`, VeeValidate `4.15.1`, Zod, shared dark/light token layer, unit tests, Playwright smoke test, and verified `typecheck` + `lint` + `build`
- Tightened frontend implementation rule: no `scoped style` in Vue SFC, migrated Step 3 files off `scoped`, and added an automatic lint guard via `frontend/scripts/check-no-scoped-style.mjs`
- Refactored frontend styles into centralized `src/styles/**/*.scss`, removed all `<style>` blocks from Vue SFC files, and verified lint/test/build with Sass enabled
- Completed Phase 1 Step 4 Docker dev: added `docker-compose.yml`, backend/frontend Dockerfiles, root `.dockerignore`, customizable host port mapping, verified container health for backend/frontend/postgres/minio, and verified backend/frontend HTTP responses from inside containers
- Completed Phase 1 Step 5 Docker production: converted backend/frontend Dockerfiles to multi-stage `dev`/`prod`, added `docker-compose.prod.yml`, added Nginx reverse-proxy config, ensured production compose does not mount source code or expose Postgres/MinIO publicly, and verified `docker compose -f docker-compose.prod.yml config` plus production image builds
- Completed Phase 1 Step 6 Docker test profile: added `docker-compose.test.yml`, isolated test Postgres/MinIO volumes, verified `backend-test`, `frontend-test`, and `e2e-test`, and fixed Vite host allowlisting for Docker browser tests
- Completed Phase 1 Step 7 Quality gates: added a root `Makefile` with canonical backend/frontend/local/Docker commands, documented the workflow in `README.md`, and verified `make check` plus Docker browser E2E
- Fixed the mobile admin shell: moved small-screen sidebar behavior to an off-canvas overlay with backdrop, added viewport-aware layout state, tightened mobile spacing, and re-verified Docker browser E2E
- Fixed mobile shell width drift: topbar, page header, content wrapper, and dashboard cards now use symmetric mobile gutters with full-width constraints and clipped horizontal overflow
- Added system-wide typography and time baseline: `Be Vietnam Pro` is the default UI font, `.env.example` now carries `APP_TIMEZONE` and `VITE_APP_TIMEZONE`, and project rules now require explicit `Asia/Ho_Chi_Minh` time handling
- Fixed dashboard form overflow: PrimeVue `Owner Email` input in `Quick Filter Form` is now constrained correctly inside the card on desktop/mobile
- Added a shared admin-shell footer in `AdminLayout` with consistent product metadata and timezone display sourced from `VITE_APP_TIMEZONE`
- Added `docs/phase-2-auth-rbac-implementation-plan.md` with scope, deliverables, rollout order, acceptance criteria, risks, and test matrix for Phase 2
- Closed Phase 2 Step 1 auth strategy: added `docs/phase-2-auth-strategy-decision.md`, updated design/plan docs, and introduced hybrid auth config baseline in `.env.example` and backend settings
- Completed Phase 2 Step 2 scaffold at code level: added auth/RBAC ORM models, metadata registration, a first Alembic revision, and a schema metadata test for users/roles/permissions/refresh_tokens/audit_logs
- Completed Phase 2 Step 3 scaffold at code level: added password hashing, JWT access-token issue/decode, refresh-token issue/refresh/revoke flow, current-user dependency, and auth core tests under `backend/app/auth/`
- Completed Phase 2 Step 4 scaffold at code level: added backend permission resolver helpers, `require_permission(...)`, eager loading of `roles -> permissions` for current-user/auth flows, and RBAC core tests
- Completed Phase 2 Step 5 scaffold at code level: added auth request/response schemas, `/api/v1/auth/login|refresh|logout|me` routes, refresh-cookie handling, and auth API contract tests
- Completed Phase 2 Step 6 scaffold at code level: added baseline auth seed constants, idempotent auth/RBAC seed service, seed-related settings/env vars, `backend/scripts/seed_auth_rbac.py`, and `Makefile`/README wiring for initial admin/bootstrap data
- Completed Phase 2 Step 7 scaffold at code level: added frontend auth API client, shared HTTP helper, auth and permission stores, router guards, login page, forbidden page, logout UI, and verified anonymous redirect to login through Docker Playwright E2E
- Hardened the frontend API boundary for auth: split backend DTOs from frontend domain models and added `auth.mappers.ts` so future backend contract changes stay localized to the API layer
- Completed Phase 2 Step 8 scaffold at code level: added `AuditLogService`, wired auth routes to emit audit events for login success/failure, session refresh, and logout, and re-verified backend `pytest` + `ruff` + `mypy`
- Completed the remaining Step 8 scope from the Phase 2 plan: added minimal admin mutation endpoints for `create user` and `update user roles`, protected them with `require_permission(...)`, emitted audit events for both, and re-verified backend `pytest` + `ruff` + `mypy`
- Completed full Users CRUD interface (listing, pagination, filter, search, sorting, details, creation, updates, and deletion) along with the system Roles list endpoint, and verified with passing frontend lint, typecheck, unit tests, and API contract unit tests.
- Completed Phase 4 Storage & Audit integration: implemented database `File` ORM model, Alembic table migration, lifespan auto-bootstrap bucket setup, file services, files API controllers (with FastAPI streaming download proxy), audit logging triggers, frontend types/mappers/api client, vue composables, responsive FilesPage with FileUpload interface, and verified with passing backend unit/contract tests and frontend checks.
- Re-verified on 10/06/2026 that Phases 2, 3, and 4 are complete against the current design doc scope. Backend `pytest`, `ruff`, and `mypy` pass; frontend `typecheck`, `lint`, `test:unit`, and `build` pass.
- Fixed Docker dev startup regression on 10/06/2026: removed the default Redis host-port binding from `docker-compose.yml`, so `docker compose up --build` no longer fails when local port `6379` is already occupied.
- Fixed frontend auth login regression on 10/06/2026: added backend CORS middleware and a preflight regression test so browser `OPTIONS /api/v1/auth/login` no longer fails with `405`.
- Fixed dev auth bootstrap/runtime on 10/06/2026: corrected Alembic Docker path + async env, fixed duplicate enum creation in the first migration, fixed async relationship initialization in auth seed, fixed enum value persistence for `UserStatus`, ran migrations successfully, and seeded the local admin account.
- Implemented user profile fields (`full_name` and `avatar_url`) on 11/06/2026: updated initial Alembic migration directly, mapped attributes in User ORM model, exposed them through backend Pydantic schemas (UserResponse, CurrentUserResponse), updated UserAdminService, populated them in auth endpoints, and wired frontend types, normalizer mappers, useUsersPage composable form states/validation, and UsersPage DataTable columns and dialog fields.
- Audited required-field markers on 11/06/2026 and extended the red-asterisk convention beyond Login/Users to Roles, Files upload, and the dashboard Quick Filter form. Project rules and design docs now treat the marker as a mandatory form UX contract.
- Replaced manual avatar URL entry in Users management with image upload flow on 11/06/2026: added backend `/api/v1/users/avatar-upload`, reused file storage + MinIO, updated Users create/edit dialogs to upload images instead of typing links, and verified backend route tests in-container plus frontend lint/typecheck/unit/build.
- Fixed avatar preview URL regression on 11/06/2026 by switching backend file/avatar/job payloads from absolute URLs to relative `/api/v1/...` paths, preventing browser `ERR_NAME_NOT_RESOLVED` when the backend runs behind Docker/Vite proxy hostnames.
- Refined the shared topbar on 11/06/2026: removed inline email/logout controls, added avatar-trigger dropdown account menu, and introduced a minimal authenticated `/profile` page so the new `Hồ sơ` action has a valid destination.
- Fixed topbar avatar stale-state on 11/06/2026: when Users edit saves the currently logged-in account, the flow now refreshes `authStore.currentUser` immediately so the shell avatar updates without page reload.
- Fixed mobile topbar visibility regression on 11/06/2026: removed overflow clipping from sticky admin-shell ancestors, moved horizontal overflow protection to `body/#app`, added router scroll restoration, and re-verified frontend lint/typecheck/build.
- Extended the mobile topbar fix on 11/06/2026 to CRUD pages: Users, Roles, and Files now constrain header/filter/action widths correctly on mobile, stack action buttons full-width, and let DataTable wrappers scroll horizontally inside the card instead of stretching the whole page.
- Started and substantially implemented `Phase 5: Hardening` on 11/06/2026: added backend security headers middleware, in-memory rate limiting for sensitive endpoints, query caps for list APIs, backend hardening tests, frontend/backend coverage output, GitHub Actions CI workflow, dependency-audit targets, and performance smoke scripts.
- Re-verified Phase 5 local quality gates on 11/06/2026: backend `pytest`, `ruff`, and `mypy` pass; frontend `lint`, `typecheck`, and `test:unit` pass.
- Fixed Phase 5 tooling regressions on 11/06/2026: hardening tests no longer hang on unstable ASGI multipart/export request paths, performance smoke targets now run with the backend Python environment instead of host `python3`, and dependency-audit targets now use writable cache directories under `/tmp`.
- Started and substantially implemented `Phase 6: Production Readiness` on 11/06/2026: added backend structured JSON logging, `/metrics`, `/ready`, OpenTelemetry baseline, observability compose/config assets, production env example, backup/restore scripts, restore-drill helper, compliance gate script, and deploy/backup runbooks.
- Verified Phase 6 local gates on 11/06/2026: targeted backend tests for metrics/readiness/secret-file loading pass, full backend `pytest` pass, backend `ruff` + `mypy` pass, frontend `lint` + `typecheck` + `test:unit` pass, and production-readiness compliance script validates compose/assets.
- Updated the frontend login screen on 24/07/2026 to match the Hồng Hà HRMS reference card while preserving the existing page background and auth flow. The change keeps login form logic in `useLoginPage`, uses centralized SCSS/theme tokens, and was verified with frontend lint, typecheck, unit tests, build, Playwright screenshots for desktop/mobile, and the login E2E smoke spec.
- Standardized frontend DataTable pagination on 24/07/2026: every DataTable now defaults to 10 rows per page, exposes 10/20/30/50 row-count selection, and shows the Vietnamese current-page report text. Server-side lazy tables keep using their existing `lazyParams.limit` and `onPageChange` flow, while smaller client-side tables use local pagination.
- Đã thêm kế hoạch Phase 7 cho module Audit Log vào ngày 24/07/2026 sau khi review bằng local skill `improve-codebase-architecture` và local subagents. Kế hoạch được chia theo Slice: audit inventory/contract, backend read API và indexes, metadata sanitizer, frontend viewer, request-layer coverage, worker-layer coverage, và E2E/docs verification.
- Đã thêm guardrail tài liệu vào ngày 24/07/2026: mọi tài liệu dự án viết bằng tiếng Việt phải dùng đầy đủ dấu tiếng Việt. Kế hoạch Phase 7 Audit Log đã được viết lại theo rule này, và regression đã được ghi vào `memory-bank/bugPatterns.md`.
- Đã hoàn thành Phase 7 Slice 1 vào ngày 24/07/2026: thêm backend Audit Log read API `GET /api/v1/audit-logs`, `AuditLogAdminService`, schema response, keyset cursor theo `(created_at, id)`, filter exact, boundary ngày theo `Asia/Ho_Chi_Minh`, migration index phục vụ viewer, và sanitizer metadata trung tâm dùng cho cả write path/read response. Targeted Slice 1 tests pass, backend ruff/format/mypy/security pass; full `make backend-check` bị ngắt ở pha full pytest do test suite treo trong môi trường hiện tại.
- Đã hoàn thành Phase 7 Slice 2 vào ngày 24/07/2026: mở rộng `AuditLogService` thành deep module cho metadata sanitizer allowlist và `AuditLogContext.from_request(...)`, chuẩn hóa IP/request id cho auth/users/roles/files/jobs, thêm guardrail không dùng `_extract_client_ip` hoặc `request.client.host` trực tiếp trong API route, và xác minh bằng targeted tests, backend ruff/format/mypy/security.
- Đã hoàn thành Phase 7 Slice 3 vào ngày 27/07/2026: thêm frontend Audit Log viewer với route/sidebar gated bằng `audit.read`, type/API/mapper/composable riêng, filter actor/action/entity/request/date range, DataTable lazy với rows mặc định 10 và options 10/20/30/50, paginator tuần tự prev/next trên keyset cursor, metadata dialog chỉ render metadata đã sanitize từ backend, và unit tests cho mapper/composable/page/router/sidebar. `make frontend-check` và `npm --prefix frontend run build` pass.
- Đã hoàn thành Phase 7 Slice 4 vào ngày 27/07/2026: mở rộng audit coverage ở request-layer cho `POST /backups/now`, `POST /backups/schedules`, `PUT /backups/schedules/{schedule_id}` và `DELETE /backups/schedules/{schedule_id}`; chuyển các backup mutation sang route-level commit để audit và mutation cùng transaction; manual backup chỉ enqueue Redis sau commit thành công; thêm tests cho action/entity/metadata/context, failure path không ghi success event, route `204` trả body rỗng, RBAC `backups.write`, và sanitizer allowlist metadata backup.
- Đã hoàn thành Phase 7 Slice 5 vào ngày 27/07/2026: mở rộng worker-layer audit coverage cho import/export/backup completed/failed events; thêm metadata allowlist cho worker keys; đảm bảo terminal audit ghi cùng transaction với terminal status; không đưa raw exception, stderr, path, storage path, CSV content, token hoặc password vào audit metadata; thêm idempotency guard chỉ bỏ qua retry khi export/backup đã terminal `completed`/`failed`; và sửa backup success notification failure không còn làm đổi status từ `completed` sang `failed`.
- Đã hoàn thành Phase 7 Slice 6 vào ngày 27/07/2026: thêm Docker Playwright E2E cho Audit Logs, kiểm chứng admin tạo mutation thật rồi xem event trong viewer, mở metadata đã sanitize, chụp screenshot desktop/mobile, và kiểm chứng user thiếu `audit.read` bị API audit logs chặn `403`; Docker test targets nay dùng `run --build --rm` để tránh image stale.
- Đã cải thiện Audit Logs sau phản hồi UX vào ngày 27/07/2026: bảng mặc định không còn phơi bày `Actor user ID`, `Entity ID`, `Request ID` và IP như thông tin chính; các ID kỹ thuật được chuyển vào bộ lọc nâng cao hoặc dialog chi tiết. Metadata user create/update/role update nay có `changes` gồm nhãn trường, `old_value` và `new_value`, nên việc thêm/thay ảnh đại diện qua form user được audit thành thay đổi `Ảnh đại diện` rõ ràng.
- Đã chuẩn hóa cơ chế ghi IP audit vào ngày 27/07/2026: thêm helper `resolve_client_ip(...)` dùng chung cho audit log và rate limit; chỉ tin `X-Forwarded-For`/`X-Real-IP` khi socket client thuộc `TRUSTED_PROXY_CIDRS`; production Docker cố định subnet compose mặc định `172.30.0.0/24` và Nginx reset `X-Forwarded-For` bằng `$remote_addr`; tài liệu vận hành đã ghi rõ cấu hình cho cloud Docker, LAN/VPN direct và LAN/VPN qua proxy.
- Đã ghi memory chuẩn hóa cách chạy Playwright vào ngày 27/07/2026: browser E2E chính thức của repo chạy bằng `make docker-test-e2e`, thông qua service `e2e-test` và Dockerfile target `e2e` có sẵn Chromium/system deps/E2E credentials/base URL Docker. Không dùng container frontend dev hoặc lệnh Playwright rời để kết luận các luồng auth/proxy/audit/network đã ổn.
- Đã chuyển Dashboard admin khỏi nội dung smoke/scaffold vào ngày 27/07/2026: màn hình chính dùng tên `Bảng điều khiển`, sidebar cũng dùng nhãn này, nội dung tập trung vào tổng quan vận hành cho tài khoản, vai trò, tệp tin, sao lưu và nhật ký audit; form mô phỏng quick filter được thay bằng panel truy cập nhanh theo quyền.
- Đã cập nhật yêu cầu nghiệp vụ Quotify ngày 27/07/2026 theo phạm vi đơn giản hóa đã thống nhất: bổ sung phiếu/dòng/phiên bản báo giá, quy tắc tỷ giá USD bán ra Vietcombank, nhập lại báo giá, lịch sử ghi chú, giả định điều kiện so sánh tương đương, checkbox chốt mua và dashboard cơ bản; đồng thời tạo `CONTEXT.md` làm bảng thuật ngữ nghiệp vụ.
- Đã chốt fallback tỷ giá Quotify ngày 27/07/2026: khi ngày nhận là hiện tại nhưng không lấy được tỷ giá Vietcombank, người dùng được nhập tay với lý do bắt buộc; nguồn, người nhập, thời điểm và lý do phải được lưu để audit.
- Đã tạo và review hai vòng kế hoạch triển khai Quotify theo 11 giai đoạn tại `docs/quotify/quotify-implementation-plan.md` ngày 27/07/2026. Kế hoạch bao phủ danh mục, NCC, import CSV, tỷ giá/cấu hình, vòng đời phiếu-version-line, giao diện nhập liệu, lịch sử ghi chú, DataTable, dashboard, E2E/hardening; đồng thời chốt guardrail về provenance giá, concurrency version, file authorization và phân tích theo thời điểm dữ liệu thực sự tồn tại trong hệ thống.
- Đã hoàn thành Phase 0 Quotify ngày 27/07/2026: đổi runtime branding/cookie/metric/package metadata sang Quotify, khôi phục `.env.production.example`, bổ sung permission seed hiện có và permission Quotify dự kiến, thêm `backend/tests/test_permission_inventory.py`, chốt DG-01/DG-02/DG-03 trong `Requirements.txt`, `CONTEXT.md` và kế hoạch triển khai. Kiểm chứng đã chạy: permission inventory trực tiếp bằng Python, `py_compile`, `git diff --check`, grep branding cũ; backend pytest host-side bị chặn do sandbox DNS không tải được `hatchling`, frontend typecheck/lint host-side bị chặn do thiếu `node_modules`.
- Đã hoàn thành Phase 1 Quotify ngày 27/07/2026: thêm danh mục `Loại vật tư` và `Vật tư` từ database tới giao diện, gồm migration, ORM model, schema, service, API CRUD/list/filter/sort/paginate, RBAC, audit event có metadata `changes`, route/menu frontend, API mapper, composable, DataTable lazy, form validation và SCSS responsive. Kiểm chứng đã chạy: `python3 -m py_compile` cho backend file mới, `git diff --check`, Docker backend targeted test `8 passed, 1 skipped`, Docker `uv run alembic upgrade head`, Docker frontend `npm run typecheck && npm run lint`.
- Đã thêm seed dữ liệu Quotify ngày 27/07/2026: `seed_quotify.py` tạo idempotent danh mục loại vật tư và 27 vật tư phổ biến ngành sản xuất thức ăn chăn nuôi; ngày 04/08/2026 danh sách loại vật tư seed được cập nhật thành `Nguyên liệu`, `Vi lượng`, `Bao bì`, `Premix`. Docker dev/E2E chạy script này sau auth seed, Makefile `seed` cũng chạy cả auth seed và Quotify seed. Kiểm chứng ban đầu đã chạy: Docker targeted test `10 passed, 1 skipped`, `uv run alembic upgrade head && uv run python scripts/seed_quotify.py` hai lần, `uv run ruff check`, `uv run mypy` phần mới, `py_compile`, `git diff --check`.
- Đã hoàn thành Phase 2 Quotify ngày 27/07/2026: thêm danh mục `Nhà cung cấp` với backend migration/model/schema/service/API cho `suppliers`, `supplier_contacts`, `supplier_materials`; ràng buộc unique mã NCC và unique cặp NCC-vật tư; lookup NCC active theo vật tư; không gắn vật tư inactive; audit metadata có `changes` old/new. Frontend có API/mapper/type/composable/page/style cho `/catalog/suppliers`, menu `Nhà cung cấp`, DataTable lazy với paging chuẩn và dialog quản lý thông tin chính, liên hệ, vật tư cung cấp. Kiểm chứng đã chạy: `python3 -m py_compile`, `git diff --check`, Docker `alembic upgrade head`, Docker frontend `npm run typecheck && npm run lint`, Docker backend targeted test `27 passed, 1 skipped`.
- Đã hoàn thành Phase 3A Quotify ngày 27/07/2026: mở rộng `ImportJob` bằng `entity_type` và `task_name`, thêm migration backfill mặc định `users/import_users_task`, tách tạo job khỏi enqueue Redis, route user import commit file/job/audit start trước khi enqueue, list/detail user import luôn filter `entity_type="users"`, worker import user đọc CSV streaming theo chunk và có guard không xử lý nhầm entity type khác. Kiểm chứng đã chạy: `python3 -m py_compile`, `git diff --check`, Docker `alembic upgrade head`, Docker backend targeted test `53 passed, 1 skipped`, Docker backend ruff/mypy, full Docker backend pytest `179 passed, 1 skipped`, Docker frontend `npm run typecheck && npm run lint`.
- Đã hoàn thành Phase 3B Quotify ngày 27/07/2026: thêm catalog import API `/catalog-imports/{entity_type}`, `/catalog-imports/{job_id}`, `/catalog-imports/{job_id}/error-file`, `/catalog-imports/templates/{entity_type}`; `entity_type` chỉ allowlist `material_types|materials|suppliers` và tự map permission `*.import`; worker `import_catalog_task` validate header, đọc CSV streaming, upsert theo mã, row lỗi vào `errors_json`, partial success vẫn `completed`; frontend 3 trang danh mục có `Import CSV`, tải template, upload, theo dõi tiến độ và tải file lỗi. Kiểm chứng đã chạy: `python3 -m py_compile`, `git diff --check`, Docker backend targeted test `27 passed`, Docker backend ruff/mypy, full Docker backend pytest `187 passed, 1 skipped`, Docker frontend `npm run typecheck && npm run lint`, và `make docker-test-e2e` pass `3 passed`.
- Đã hoàn thành Phase 4 Backend Quotify ngày 28/07/2026: thêm `quotify_settings` model/migration/service/schema, settings/env cho Vietcombank URL/timeout/retry/rate limit, adapter `VietcombankExchangeRateClient` bọc `httpx`, parser XML fixture, helper `GET /exchange-rates/usd-sell/today`, API read/update cấu hình chi phí quy đổi, RBAC, rate limit, audit metadata `changes` old/new và allowlist sanitizer. Quy đổi dùng `Decimal`, `ROUND_HALF_UP`, scale 2 chữ số; business today dùng `Asia/Ho_Chi_Minh`; không persist snapshot tỷ giá độc lập. Kiểm chứng đã chạy: `python3 -m py_compile`, Docker targeted test `21 passed, 1 skipped`, Docker ruff/mypy, Docker Alembic upgrade head, full Docker backend pytest `199 passed, 1 skipped`.
- Đã hoàn thành Phase 4 Frontend Quotify ngày 28/07/2026: thêm type/mapper/API cho `/exchange-rates/usd-sell/today`, `/quotify-settings`, `/quotify-settings/conversion-cost`; thêm composable và trang `/quotify-settings` để xem tỷ giá USD bán ra hôm nay, xem/lưu chi phí quy đổi VNĐ/KG theo quyền; thêm menu `Cấu hình quy đổi` trong nhóm `Báo giá`, SCSS tập trung và unit test mapper. Đồng thời hoàn thiện component `ExchangeRateField.vue` xử lý tỷ giá auto/manual/fallback, viết styles SCSS tập trung, và bổ sung toàn bộ bộ unit tests cho `useQuotifySettingsPage`, `QuotifySettingsPage` và `ExchangeRateField` đạt coverage đầy đủ.
- Đã sửa lỗi runtime trang `/quotify-settings` ngày 28/07/2026: chạy migration cho Docker dev đang sống, thêm migration seed default `quotify_settings`, commit fallback `get_or_create` trong route đọc, thêm regression test API và E2E browser mở `Cấu hình quy đổi` để chặn lỗi `500`. Kiểm chứng đã chạy: Docker targeted backend tests `5 passed`, Docker backend ruff/mypy targeted, Docker frontend typecheck/lint, kiểm HTTP qua Vite proxy `200`, DB có đúng một dòng default, và `make docker-test-e2e` pass `3 passed`.
- Hoàn thành Phase 5 Backend ngày 28/07/2026: Tạo database models (Quote, QuoteVersion, QuoteLine), Alembic migrations, backend services (QuoteService, QuotePricingService), schemas, API endpoints (/api/v1/quotes), và bộ unit tests targeted test_quote_lifecycle.py.
- Hoàn thành Phase 6 Giao diện nhập và xem phiếu báo giá (Frontend) ngày 28/07/2026: thêm type/mapper/API client cho Quote/Version/Line, composables useQuoteEditor và useQuoteDetail, màn hình nhập liệu và xem chi tiết responsive, styles SCSS tập trung, đăng ký routes/menu, và đầy đủ unit tests mappers/composables.
- Hoàn thành tối ưu hóa UX nhập báo giá nhiều tháng ngày 28/07/2026: Triển khai Phương án 2 (Nhân bản nhanh dòng vật tư - Clone/Duplicate Line) trong `useQuoteEditor.ts` và `QuoteEditorPage.vue` (cả desktop/mobile view).
- Đã sửa các lỗi phát hiện khi vận hành báo giá ngày 28/07/2026: Sửa lỗi hiển thị nguồn tỷ giá 'Chưa xác định' cho các dòng thêm sau bằng cách gán đúng `exchangeRateSource` trong `useQuoteEditor.ts`; Sửa lỗi `hasPermission` trong `QuoteDetailPage.vue`; Sửa lỗi `getTodayString is not defined` trong `QuoteEditorPage.vue`; Cải tiến `AdminLayout.vue` tự động highlight active menu 'Nhập báo giá' cho các sub-routes con.
- Hoàn thành Phase 7 Backend (Ghi chú thị trường & Lịch sử chỉnh sửa) ngày 28/07/2026: Viết test và tích hợp thư viện HTML sanitizer `nh3` an toàn; Tạo các database models `QuoteNote`, `QuoteNoteRevision`; Tạo Alembic migration `20260728_1100_create_quote_notes.py`; Cài đặt `QuoteNoteService`; Bổ sung các API endpoints `GET /api/v1/quotes/{quote_id}/notes` và `PUT /api/v1/quotes/{quote_id}/notes` cùng với bộ unit tests cho service và API endpoints.
- Hoàn thành Phase 7 Frontend (Ghi chú thị trường) ngày 29/07/2026: Tích hợp PrimeVue Editor, Quill rich text engine; Hiển thị danh sách ghi chú dạng bình luận Facebook (Facebook comment style) sắp xếp theo thời gian, bỏ số hiệu phiên bản; Thêm nút "Thêm" ghi chú mới; Thêm nút "Sửa" và "Xóa" cho từng ghi chú để sửa/xóa trực tiếp (inline); Bổ sung API endpoints PATCH/DELETE note revisions ở backend và các api client/composable/unit tests tương ứng ở frontend.
- Hoàn thành Phase 9 Backend (Dashboard Phân Tích Giá) ngày 29/07/2026: thêm service aggregate `QuotifyDashboardService`, schema `quotify_dashboard`, router `/api/v1/dashboard/quotify/entry-kpis` và `/api/v1/dashboard/quotify/price-trends`; endpoint dùng `dashboard.read`, chỉ lấy `QuoteVersion.status='confirmed'`, tính MIN/MAX/AVG trên giá quy đổi đã đóng băng ở `QuoteLine`, KPI đếm distinct `Quote.id` theo người tạo phiếu ban đầu, và trả purchase context tại/sau thời điểm tick chốt mua dựa trên `confirmed_at` so với `purchase_marked_at`. Kiểm chứng đã chạy: targeted dashboard tests `4 passed`, permission inventory `3 passed`, combined targeted `7 passed`, ruff targeted pass với cache `/tmp`, `py_compile` pass; mypy targeted gặp internal error của mypy 1.20.2 trên Python 3.14; cụm quote mở rộng hiện có lỗi fake-session/quote-note ngoài diff Phase 9.
- Hoàn thành Phase 9 Frontend (Dashboard Phân Tích Giá) ngày 29/07/2026: thay dashboard vận hành/scaffold bằng dashboard phân tích báo giá Quotify; thêm type/mapper/API client `quotify-dashboard`, composable `useDashboardPage`, filter vật tư/kỳ giao hàng/khoảng ngày nhận, KPI cards MIN/MAX/TRUNG BÌNH/tổng báo giá, biểu đồ PrimeVue Chart + `chart.js`, marker đã chốt mua, bảng KPI người nhập và bảng bối cảnh chốt mua. Style nằm trong `dashboard-page.scss`, dùng theme tokens cho dark/light mode và thêm semantic tokens `success/info/warning`. Kiểm chứng đã chạy: unit test mục tiêu `3 files, 6 passed`, ESLint mục tiêu pass, `lint:styles` pass, `git diff --check` pass, `npx vite build` trong Docker pass. `make docker-test-frontend` và `npm run typecheck` toàn repo đang bị chặn bởi nợ lỗi frontend có sẵn ngoài diff Phase 9.
- Cải thiện Dashboard Quotify ngày 29/07/2026 sau phản hồi UX: chart không còn mặc định lấy tất cả vật tư mà tự chọn `Ngô hạt` (`CORN`) sau khi load lookup; `Xóa lọc` quay lại default này; thêm filter `Loại NCC` (`Nội địa`/`Quốc tế`) từ UI xuống backend cho cả KPI, summary, trend points và purchase context. Kiểm chứng đã chạy: backend dashboard targeted tests `4 passed`, frontend dashboard targeted unit tests `6 passed`, backend ruff targeted pass, frontend ESLint mục tiêu + `lint:styles` pass, `npx vite build` pass.
- Cải thiện ý nghĩa chart `Xu hướng` ngày 29/07/2026: theo góc nhìn Trưởng phòng Mua hàng và Data Analysis, chart chính chuyển từ trục `Ngày nhận báo giá` sang trục `Kỳ hàng về`; các báo giá cùng kỳ được gom thành MIN/MAX/AVG, marker chốt mua nằm trên đúng kỳ hàng về, còn ngày nhận báo giá/NCC/Loại NCC hiển thị trong tooltip. Backend trả thêm `supplier_type` ở trend point. Kiểm chứng đã chạy: backend dashboard targeted tests `4 passed`, frontend dashboard targeted unit tests `6 passed`, backend ruff targeted pass, frontend ESLint mục tiêu + `lint:styles` pass, `npx vite build` pass.
- Đã debug và sửa cụm lỗi backend phát hiện khi làm Phase 9 BE ngày 29/07/2026: `QuoteService` dùng `scalars()` đúng cho scalar select, `QuoteNoteService` sở hữu lookup revision, route note update/delete không query session trực tiếp để lấy old content, fake AsyncSession trong tests được đồng bộ với `AsyncSession` thật, và test lifecycle cố định business today để không tự hỏng theo ngày hiện tại. Kiểm chứng đã chạy: cụm quote lifecycle/query/list/note tests `21 passed`, dashboard backend + permission inventory `6 passed, 1 skipped`, ruff mục tiêu `I/F401/F821` pass.
- Đã sửa lỗi edit/lưu `Nhà cung cấp` trả 500 ngày 29/07/2026: `SupplierAdminService.update_supplier(...)` không còn replace toàn bộ `supplier_materials` bằng object mới mà sync theo diff, giữ link vật tư cũ để tránh duplicate unique pair trong flush. Kiểm chứng đã chạy: supplier service/API tests `8 passed`, runtime dev API login + GET supplier lỗi trong log + PUT cùng payload hai lần đều `200`, ruff mục tiêu `I/F401/F821` pass, `py_compile` pass.
- Đã sửa lỗi trang `Nhà cung cấp` bấm Edit lần đầu sau khi tìm `W` chỉ nháy rồi lần hai mới mở dialog ngày 29/07/2026: search DataTable lazy được debounce 250ms, response cũ bị bỏ qua bằng `latestFetchId`, nút sửa/xóa trong hàng dùng `@click.stop`, thêm unit test `useSuppliersPage` và E2E spec supplier để chạy khi quality gate frontend toàn repo được dọn. Kiểm chứng đã chạy: unit test mục tiêu `3 passed`, ESLint mục tiêu pass, `lint:styles` pass, `git diff --check` pass; Docker E2E chuẩn bị chặn bởi typecheck cũ ở module báo giá đã ghi trong bug memory.
- Đã sửa lỗi thứ tự dòng ở trang chi tiết báo giá ngày 29/07/2026: thêm `quote_lines.line_order`, migration backfill dữ liệu cũ theo nhóm mã vật tư rồi kỳ giao hàng, service ghi thứ tự dòng theo index payload khi tạo quote/tạo version/sửa draft, relationship `QuoteVersion.lines` sort theo `line_order`, API/frontend mapper trả `lineOrder`. Quote người dùng báo `1dfc8c2a-8092-4731-a689-b8509084e749` đã được kiểm qua API dev và trả thứ tự `Ngô hạt` theo tháng 08-02 rồi tới `Khô dầu đậu nành` theo tháng tăng dần. Kiểm chứng đã chạy: Alembic upgrade head dev pass, quote lifecycle tests `8 passed`, frontend mapper tests `4 passed`, py_compile pass, Ruff migration pass, Ruff hẹp `F401/F821` pass, ESLint frontend mục tiêu pass, `git diff --check` pass.
- Đã sửa định dạng số tiền trên Dashboard Quotify ngày 29/07/2026: các giá tiền dùng dấu phẩy ngăn phần nghìn và dấu chấm ngăn phần thập phân, ví dụ `10,200.00 VNĐ/KG`, thay vì format `vi-VN` kiểu `10.200,00`. Kiểm chứng đã chạy: dashboard unit tests `4 passed`, ESLint mục tiêu pass, `lint:styles` pass, `git diff --check` pass.
- Đã sửa responsive mobile cho trang `/quotes` ngày 29/07/2026: desktop vẫn dùng DataTable, mobile chuyển sang card list riêng có thông tin chính của từng dòng báo giá, trạng thái, giá quy đổi, nút chi tiết và paginator mobile. Đồng thời chuẩn hóa DatePicker filter sang `Date | null` rồi convert query khi gọi API. Kiểm chứng đã chạy: ESLint mục tiêu pass, `useQuotesPage` unit tests `3 passed`, `lint:styles` pass, `git diff --check` pass, Playwright mobile spec trên dev container pass `1 passed`. Docker E2E chuẩn bị chặn bởi typecheck cũ ngoài phạm vi trong cụm báo giá/editor đã ghi ở bug memory.
- Đã sửa lại quy tắc hiển thị giá trên trang `/quotes` ngày 29/07/2026: `Giá gốc` giữ nguyên tiền tệ và đơn vị của báo giá nhà cung cấp (`VND/KG` hoặc `USD/MT`), còn `Giá quy đổi` mới là giá chuẩn hóa `VNĐ/KG`. Kiểm chứng đã chạy: ESLint mục tiêu pass, `lint:styles` pass, Playwright mobile spec `1 passed`, `git diff --check` pass.
- Đã chuẩn hóa nhãn tiền Việt trên giao diện báo giá ngày 29/07/2026: không dùng `đ/KG`, `₫ / KG` hoặc hậu tố `đ`; các giá theo kg hiển thị bằng `VNĐ/KG`, trong khi báo giá gốc `USD/MT` vẫn giữ nguyên `USD/MT`. Kiểm chứng đã chạy: ESLint hẹp pass, `lint:styles` pass, Playwright mobile spec `1 passed`, quét `frontend/src` không còn mẫu nhãn cũ.
- Đã thêm filter dòng vật tư trên trang chi tiết báo giá ngày 29/07/2026: trang `/quotes/{id}` có global search, filter `Tên vật tư`, filter `Tháng giao`, counter số dòng đang hiển thị và nút `Xóa lọc`; filter chạy client-side trên `activeVersion.lines`, giữ nguyên thứ tự dòng đã nhập. Kiểm chứng đã chạy: Playwright spec trên quote mẫu `1dfc8c2a-8092-4731-a689-b8509084e749` pass, ESLint spec pass, `lint:styles` pass, `git diff --check` pass.
- Đã cải tiến hiệu năng backend cho DataTable `/quotes` ngày 29/07/2026: API list báo giá cap `limit` ở 100 và chặn `offset` âm bằng FastAPI `Query`, `QuoteQueryService` tự clamp limit/offset khi gọi trực tiếp, `COUNT` không còn bọc subquery chọn toàn bộ cột mà count trực tiếp theo `QuoteLine.id`, sort có tie-breaker `QuoteLine.id`, và migration mới thêm index tổng hợp cùng GIN trigram cho global search vật tư/NCC. Kiểm chứng đã chạy: targeted quote list/query tests `4 passed`, Ruff hẹp pass, Alembic upgrade head trong Docker dev pass. Giới hạn còn lại: UI vẫn dùng offset pagination nên nếu cần nhảy tới page rất sâu trên hàng triệu dòng thì nên có thêm cursor/keyset mode riêng.
- Đã chốt và khóa phân quyền menu `Cấu hình quy đổi` ngày 29/07/2026: menu vẫn nằm trong nhóm `Báo giá`, route/sidebar chỉ hiện với `quotify_settings.read`, nút lưu dùng `quotify_settings.update`, role hệ thống `user` không được cấp quyền cấu hình; migration `20260729_0810` thu hồi `quotify_settings.read/update` khỏi role `user` nếu DB cũ từng bị cấp nhầm. Kiểm chứng đã chạy: Alembic upgrade head pass, unit sidebar `8 passed`, permission inventory `2 passed, 1 skipped`, truy vấn DB dev role `user` trả `0 rows` cho `quotify_settings.%`.
- Đã thêm seed user Quotify ngày 29/07/2026: `seed_quotify.py` tạo idempotent 7 tài khoản phòng Thu Mua với mật khẩu mặc định `Hongha@123`, trạng thái `active`, role mặc định `user`; email Phạm Thị Trang dùng đúng `phamthitrang@honghafeed.com.vn` và seed có `legacy_emails` để tự đổi bản cũ `phamthitrang@honghafeed.com` sang email mới mà không tạo trùng. Seed không ghi đè mật khẩu user đã tồn tại. Kiểm chứng đã chạy: unit test seed `3 passed`, Ruff mục tiêu pass, mypy mục tiêu pass, chạy seed DB dev xác nhận còn đúng bản `.com.vn`.
- Đã bổ sung widget Dashboard “Tình hình nhập báo giá theo tuần” ngày 29/07/2026: backend thêm endpoint `/api/v1/dashboard/quotify/weekly-entry-activity` dùng `dashboard.read`, tuần được chuẩn hóa Thứ Hai-Chủ nhật theo `Asia/Ho_Chi_Minh`, đếm phiếu báo giá đã xác nhận theo `Quote.created_at` và `Quote.created_by_id`, trả cả active user có `0` phiếu để frontend highlight warning. Frontend thêm filter `Tuần`/`Người nhập`, KPI `Báo giá tuần`/`User đã nhập`/`User chưa nhập`, bar chart ngang theo user và bảng chi tiết trạng thái. Kiểm chứng đã chạy: backend dashboard tests `6 passed`, frontend dashboard unit tests `8 passed`, backend Ruff/mypy mục tiêu pass, frontend ESLint mục tiêu pass, `lint:styles` pass, `npx vite build` pass; `npm run build` đầy đủ vẫn bị chặn bởi nợ typecheck cũ ngoài Dashboard.
- Đã cập nhật auth/RBAC seed ngày 30/07/2026: thêm `USER_ROLE_PERMISSION_CODES` để role hệ thống `user` được cấp quyền `dashboard.read`, CRUD `Loại vật tư`, CRUD `Vật tư`, CRUD `Nhà cung cấp`, và các quyền báo giá đang có (`quotes.read`, `quotes.create`, `quotes.update`, `quotes.mark_purchased`). Không thêm `quotes.delete` vì codebase chưa có permission/route xóa báo giá. Kiểm chứng đã chạy: permission inventory `3 passed, 1 skipped`, Ruff/mypy mục tiêu pass, `seed_auth_rbac.py` chạy thành công trong Docker dev, truy vấn DB xác nhận role `user` có đủ 17 quyền mong muốn.
- Đã sửa lỗi role `user` không lấy được tỷ giá Vietcombank khi nhập báo giá ngày 30/07/2026: nguyên nhân là endpoint tỷ giá yêu cầu `exchange_rates.read` nhưng seeder role `user` chưa cấp quyền phụ trợ này; đã thêm vào `USER_ROLE_PERMISSION_CODES`, cập nhật permission inventory test, chạy lại `seed_auth_rbac.py`, và kiểm chứng tài khoản `phamthitrang@honghafeed.com.vn` gọi `/api/v1/exchange-rates/usd-sell/today` trả `200` giống Admin.
- Đã bổ sung cơ chế bản điều chỉnh báo giá ngày 30/07/2026: version đã xác nhận không sửa trực tiếp; tạo bản điều chỉnh bắt buộc có `correction_reason`, bản điều chỉnh ở `draft` đến khi confirm, và khi confirm thì các version `confirmed` cũ của cùng quote chuyển sang `superseded` với metadata `superseded_at`, `superseded_by_id`, `superseded_by_version_id`. Danh sách báo giá mặc định loại `superseded`; dashboard phân tích tiếp tục chỉ đọc `confirmed` nên không dùng báo giá sai đã bị thay thế. Frontend đổi action thành `Tạo bản điều chỉnh`, thêm field bắt buộc `Lý do điều chỉnh`, hiển thị trạng thái `Đã bị thay thế` và khóa tick chốt mua trên version không còn hiệu lực.
- Đã chốt và triển khai quy tắc tỷ giá cho bản điều chỉnh ngày 30/07/2026: nếu `Ngày nhận báo giá` giữ nguyên như version hiệu lực cũ thì dùng lại snapshot tỷ giá/nguồn/chi phí của dòng cũ và chỉ tính lại giá quy đổi theo giá gốc mới; nếu đổi sang ngày nghiệp vụ hiện tại thì lấy tỷ giá Vietcombank tự động. Frontend không còn tự ép ngày nhận về hôm nay khi tạo bản điều chỉnh và giữ snapshot rate cho dòng clone. Kiểm chứng đã chạy: backend quote lifecycle/query/dashboard tests `16 passed`, frontend quote unit tests `14 passed`, backend Ruff `I/F/B` mục tiêu pass, frontend style lint pass, `npx vite build` pass.
- Đã bổ sung ràng buộc ownership cho mutation phiếu báo giá ngày 31/07/2026: role `user` chỉ được sửa/confirm/upload file/tick chốt mua/tạo bản điều chỉnh trên phiếu do mình tạo; role `admin` thao tác được trên tất cả phiếu. Backend áp dụng helper ownership chung và kiểm resource con thuộc đúng quote; frontend trang chi tiết chỉ hiển thị action mutation lõi khi user có quyền và là chủ phiếu hoặc Admin. Cập nhật cùng ngày: ghi chú thị trường được tách thành luồng cộng tác theo `quote_notes.*`, không dùng ownership quote cho thao tác thêm ghi chú. Kiểm chứng đã chạy: quote ownership/note/list tests `11 passed`, quote lifecycle/query/dashboard tests `16 passed`, backend Ruff mục tiêu pass, `py_compile` pass, frontend `npx vite build` pass, `git diff --check` pass.
- Đã thêm tính năng xóa bản nháp báo giá ngày 31/07/2026: chỉ version `draft` được xóa; xóa draft điều chỉnh chỉ xóa version nháp, còn xóa draft duy nhất sẽ xóa cả phiếu nháp. Route `DELETE /api/v1/quotes/{id}/versions/{version_id}` dùng `quotes.update`, ownership guard, audit `quotes.version_deleted`, route 204 trả body rỗng và dọn tệp nguồn qua `FileAdminService` nếu có. Frontend trang chi tiết có nút/dialog `Xóa bản nháp`. Kiểm chứng đã chạy: cụm backend quote tests `32 passed`, mypy mục tiêu pass, Ruff mục tiêu pass, `useQuoteDetail.ts` ESLint pass, frontend `npx vite build` pass.
- Đã sửa metadata audit log cho action xóa bản nháp báo giá ngày 31/07/2026: `deleted_quote` không còn bị sanitizer che thành `[REDACTED]`; audit ghi thêm `version_id`, `version_number`, `received_date`, `correction_reason`, `line_count`, `deleted_scope`, `deleted_quote_id`, `source_file_id`, `source_file_cleanup`. Kiểm chứng đã chạy: audit/ownership/lifecycle backend tests `30 passed`, mypy mục tiêu pass, Ruff mục tiêu pass, `git diff --check` pass.
- Đã sửa nút `Quay lại` ở trang `Chi tiết phiếu báo giá` ngày 31/07/2026: nút này không còn điều hướng về homepage `/`, mà quay về danh sách báo giá `/quotes`. Kiểm chứng đã chạy `git diff --check`; ESLint hẹp trên `QuoteDetailPage.vue` vẫn bị chặn bởi nợ cũ trong cùng file, không phát sinh từ thay đổi route này.
- Đã gỡ tính năng `Quản lý tập tin` generic khỏi Quotify ngày 31/07/2026: xóa menu/route/page `/files`, xóa frontend API/composable/style của file manager, bỏ các endpoint generic upload/list/metadata/delete và permission `files.read`, `files.upload`, `files.delete`. Vẫn giữ file-storage core, `FileAdminService`, model `File`, mapper/type file và endpoint download `/api/v1/files/{id}/download` cho avatar, export/import và file báo giá theo ngữ cảnh nghiệp vụ. Kiểm chứng đã chạy backend targeted tests `20 passed, 1 skipped`, backend Ruff/mypy mục tiêu, frontend ESLint mục tiêu và `npx vite build` pass.
- Đã bổ sung cơ chế 404 frontend ngày 31/07/2026: thêm `NotFoundPage.vue` và catch-all route `/:pathMatch(.*)*` để URL đã gỡ như `/files` không còn làm Vue Router `No match` và màn hình trống. Route 404 dùng `requiresAuth`, nên user chưa đăng nhập vẫn về login, user đã đăng nhập thấy thông báo `Không tìm thấy trang`. Kiểm chứng đã chạy frontend ESLint mục tiêu và `npx vite build` pass.
- Đã bổ sung avatar mặc định cho user ngày 31/07/2026: nếu user có `avatarUrl` thì ưu tiên hiển thị ảnh đã upload; nếu không có thì frontend chọn ổn định một ảnh SVG nội bộ trong `public/default-avatars` theo seed `id/email/fullName`. Cơ chế này áp dụng cho topbar/sidebar, trang hồ sơ, danh sách user và preview create/edit user; không ghi avatar mặc định vào database. Kiểm chứng đã chạy frontend ESLint mục tiêu, `vue-tsc --noEmit`, style lint, `npx vite build` và `git diff --check`.
- Đã sửa lỗi user thường không thêm được ghi chú trên báo giá của người khác ngày 31/07/2026: ghi chú thị trường được tách khỏi ownership mutation phiếu báo giá. Backend dùng `quote_notes.read/create/update` cho note endpoints, `PUT /quotes/{id}/notes` không kiểm `Quote.created_by_id`; user thường chỉ sửa/xóa revision do chính mình tạo, Admin quản trị tất cả. Frontend nút `Thêm` dùng `quote_notes.create`, action sửa/xóa chỉ hiện với revision của chính user hiện tại hoặc Admin. Role `user` được seed `quote_notes.read/create/update`. Đã chạy lại seeder RBAC trong Docker dev và xác nhận role `user` có quyền note cần thiết. Kiểm chứng đã chạy backend note API + permission inventory tests, backend Ruff mục tiêu, frontend `vue-tsc`, style lint, Vite build và `git diff --check`.
- Đã bổ sung avatar người viết trong card `Ghi chú thị trường` ngày 31/07/2026: backend note response trả thêm `author_avatar_url`, frontend mapper/domain nhận field này và `QuoteDetailPage` hiển thị ảnh avatar thật nếu có, fallback sang avatar mặc định nội bộ nếu user chưa upload ảnh. Kiểm chứng đã chạy backend quote note API tests, backend Ruff mục tiêu, frontend `vue-tsc`, unit `useQuoteDetail`, style lint, Vite build và `git diff --check`.
- Đã chuẩn hóa fallback avatar ngày 31/07/2026: avatar mặc định của cùng một user được seed theo `user.id`, nên avatar ở topbar, hồ sơ, danh sách user và card `Ghi chú thị trường` thống nhất ngay cả khi user chưa upload ảnh thật. Card ghi chú không còn seed fallback bằng `revision.id`. Kiểm chứng đã chạy unit `default-avatars`, unit `useQuoteDetail`, frontend `vue-tsc`, style lint, Vite build và `git diff --check`.
- Đã tinh gọn Dashboard Quotify ngày 31/07/2026: bỏ panel trùng lặp `Người nhập - Số phiếu báo giá` ở khu phân tích giá vì nội dung đã được thể hiện đầy đủ hơn trong chart và bảng `Tình hình nhập báo giá theo tuần`. Kiểm chứng đã chạy frontend `vue-tsc`, style lint, Vite build và `git diff --check`.
- Đã chỉnh layout Dashboard Quotify ngày 31/07/2026: sau khi bỏ panel người nhập trùng lặp, chart `Kỳ hàng về - Giá theo kỳ hàng về` không còn nằm trong grid hai cột mà trở thành panel full width. Kiểm chứng đã chạy frontend `vue-tsc`, style lint, Vite build và `git diff --check`.
- Đã thiết kế lại trang `/profile` ngày 04/08/2026: thêm API self-service `POST /api/v1/users/me/avatar` để user tự đổi avatar và cập nhật ngay `authStore.currentUser`, thêm `PUT /api/v1/users/me/password` để đổi mật khẩu bằng mật khẩu hiện tại, ghi audit an toàn không lưu raw password, và làm lại UI hồ sơ thành các panel thông tin tài khoản, ảnh đại diện, đổi mật khẩu với style SCSS tập trung responsive. Kiểm chứng đã chạy backend users API tests `11 passed`, backend Ruff mục tiêu, frontend ESLint mục tiêu, style lint, `npx vite build` và `git diff --check`; `npm run build` đầy đủ vẫn bị chặn bởi nợ typecheck cũ ở cụm báo giá.
- Đã bỏ trường `Lý do nhập lùi báo giá` ngày 04/08/2026: form nhập/chỉnh báo giá chỉ hiển thị banner báo giá nhập lại khi `isBackfilled=true`, không còn ô nhập lý do; frontend không validate hoặc gửi lý do nhập lùi mới; backend không còn bắt buộc `backfill_reason` khi báo giá nhập lùi, nhưng vẫn bắt buộc đánh dấu `is_backfilled=true` để giữ phân biệt dữ liệu nhập lại. Trang chi tiết không hiển thị lý do nhập bù. Kiểm chứng đã chạy quote lifecycle tests `14 passed`, unit `useQuoteEditor` `8 passed`, backend Ruff hẹp `I/F/B`, frontend ESLint test mục tiêu, style lint và `git diff --check`; lint toàn file quote vẫn bị chặn bởi nợ cũ đã ghi trong memory.
- Đã mở rộng `Loại NCC` ngày 04/08/2026 để một nhà cung cấp có thể thuộc một hoặc nhiều loại: API supplier nhận/trả `supplier_type` dạng danh sách, DB vẫn lưu chuỗi canonical để giữ tương thích dữ liệu cũ, import CSV chấp nhận nhiều loại qua dấu phẩy/chấm phẩy, dashboard filter `Nội địa`/`Quốc tế` vẫn match NCC có cả hai loại, và form Nhà cung cấp dùng `MultiSelect`. Kiểm chứng đã chạy supplier/dashboard/catalog backend tests, backend Ruff/mypy mục tiêu, migration `alembic upgrade head`, frontend unit `useSuppliersPage`, ESLint mục tiêu, style lint và `npx vite build`; `npm run build` đầy đủ vẫn bị chặn bởi nợ typecheck cũ ở cụm báo giá.
- Đã sửa lỗi import `Loại vật tư` bằng CSV sai format ngày 05/08/2026: worker import danh mục nay ghi rõ lỗi header vào `errorSummary`, set counter `0 thành công, 1 lỗi trên 1 dòng`, tạo error report dòng 1 và audit metadata có đủ tổng dòng/lỗi. Frontend các trang import danh mục hiển thị `errorSummary` để người dùng không còn thấy trạng thái thất bại mơ hồ `0 lỗi trên 0 dòng`. Kiểm chứng đã chạy backend regression/catalog tests `29 passed`, backend Ruff/mypy mục tiêu, frontend ESLint mục tiêu, Vite build, và Playwright E2E mục tiêu cho dialog import sai header `1 passed`.

- Đã tách cấu hình "Chi phí quy đổi" thành "Thuế nhập khẩu" (%, mặc định 0) và "Chi phí làm hàng" (VNĐ/KG, mặc định 200, đổi tên từ `conversion_cost_vnd_per_kg`) ngày 10/08/2026, cùng công thức giá quy đổi USD/MT mới `(Giá USD/MT / 1000) * (1 + Thuế/100) * Tỷ giá + Chi phí làm hàng`. Đã cập nhật migration, model, service, schema, API (`PUT /quotify-settings` gộp cả hai tham số), audit allowlist, và toàn bộ frontend type/mapper/composable/UI liên quan (`QuotifySettingsPage`, `useQuoteEditor`, `QuoteDetailPage`). Đồng thời sửa bug có sẵn từ trước (không liên quan trực tiếp tới refactor) khiến `GET`/`PUT /quotify-settings` crash `MissingGreenlet` khi gọi API thật, bằng `await session.refresh(...)` sau `commit()`. Kiểm chứng đã chạy: backend `pytest` full (260 passed, 1 lỗi nợ cũ không liên quan), backend Ruff (không phát sinh lỗi mới), frontend `test:unit` full và `lint` (không phát sinh lỗi mới), `npx vite build` pass, Docker dev `alembic upgrade head` pass, và gọi API thật qua `curl` xác nhận công thức tính đúng (300 USD/MT, tỷ giá 26350, thuế 5%, chi phí 250 → giá quy đổi 8550.25 VNĐ/KG).

- Đã sửa bug tự động đăng xuất khi bấm Ctrl+R/F5 liên tục ngày 10/08/2026 (refresh token rotation race), thêm cột `replaced_by_id` trên `refresh_tokens` và khoảng dung sai `REFRESH_TOKEN_REUSE_GRACE_SECONDS=30s` cho reuse do race, không làm yếu bảo mật với token bị revoke thật (logout) hoặc quá hạn dung sai. Đồng thời cập nhật trang `Bảng báo giá` (`/quotes`): bỏ cột "Phiên bản", thêm cột "Thuế nhập khẩu" và "Chi phí làm hàng"; và sửa lỗi thứ tự dòng hiển thị sai bằng cách thêm `line_order` làm tiêu chí sắp xếp phụ trong `QuoteQueryService` (nhiều dòng nhập cùng lúc có `created_at` giống nhau nên trước đó bị xáo trộn theo `id` ngẫu nhiên). Kiểm chứng: backend `pytest` full (262 passed, 1 lỗi nợ cũ), `ruff check` không lỗi mới, `alembic upgrade head` Docker dev pass, verify `curl` thật xác nhận race trước/sau fix và token logout thật vẫn bị chặn đúng.
- Đã tìm và sửa thêm root cause thứ hai của bug tự động đăng xuất ngày 10/08/2026 (fix trước chưa đủ, user test lại vẫn còn bug): `authStore.initialize()` coi lỗi mạng (`TypeError`, gồm fetch bị hủy do điều hướng trang) giống hệt lỗi `401`/`403` thật và xóa cookie marker đăng nhập vĩnh viễn. Đã sửa: tách nhánh lỗi để chỉ `401`/`403` thật mới xóa auth state; thêm `fetch(..., {keepalive: true})` cho `/auth/refresh` để browser hoàn tất request dù trang điều hướng đi; tăng dung sai reuse token backend lên 120s. Đã verify bằng Playwright thật: reload liên tục 30 lần (2 đợt x 15, cách 150-300ms, không đợi network-idle) — session sống sót toàn bộ, 0 lỗi 401, 0 lỗi console. Backend `pytest` full pass, frontend `test:unit`/`lint`/`vite build` không lỗi mới.

- Đã triển khai tính năng import lại báo giá cũ (backfill import) ngày 10/08/2026: import CSV nhiều dòng báo giá lịch sử cùng lúc, nhóm theo `(supplier_code, received_date)` thành một phiếu, bắt buộc nhập tay tỷ giá/thuế/chi phí làm hàng lịch sử cho dòng USD/MT (không dùng cấu hình hiện hành), version tạo ra ở trạng thái `confirmed` ngay, thêm ghi chú theo dòng (`QuoteLine.note`). Route `/quote-backfill-imports` với permission riêng `quotes.backfill_import`; worker task `import_quote_backfill_task` timeout 1800s. Đã tối ưu cho quy mô ~30.000 dòng: cache mã→id 2 query, `skip_reload`, commit theo lô, 1 audit event/job. Kiểm chứng: backend `pytest` full 296 passed, `ruff`/frontend lint/typecheck/build không lỗi mới, test thật Docker dev với file 30.000 dòng xử lý xong trong ~15s không lỗi, `EXPLAIN ANALYZE` xác nhận vẫn dùng index có sẵn.

- Đã lên phương án và triển khai quy trình migrate/seed cho môi trường production ngày 10/08/2026 (`docs/quotify/plan-production-migrate-seed.md`). Rà soát thấy `scripts/seed_quotify.py` gộp chung danh mục nghiệp vụ (material types/materials, nhà cung cấp mẫu) với việc tạo 7 tài khoản người dùng thật (`@honghafeed.com.vn`/`@longhaigroup.com.vn`) dùng chung một mật khẩu hard-code plaintext trong source (`Hongha@123`) — rủi ro bảo mật thật nếu chạy tự động lên production. Nghiệp vụ đã xác nhận: danh sách nhà cung cấp trong `SUPPLIER_SEEDS` không phải dữ liệu thật (chỉ là mẫu), và 7 tài khoản Thu Mua phải tạo thủ công (không đưa vào seeder production). Quyết định cuối: production chỉ chạy `scripts/seed_auth_rbac.py` (1 admin + permissions/roles) và một script mới `scripts/seed_quotify_catalog.py` (gọi `QuotifySeedService.seed_material_catalog()` mới thêm trong `backend/app/services/quotify_seed.py` — chỉ seed material types + materials, không đụng `Supplier`/`User`); tuyệt đối không chạy `seed_quotify.py` nguyên bản lên production, và không có đường chạy `alembic downgrade` (migrate-refresh) nào cho prod. Giá trị khởi tạo `quotify_settings` đã được seed sẵn qua migration `20260728_0930_seed_default_quotify_settings.py`. Đã thêm 3 target Makefile `migrate-prod`/`seed-prod-auth`/`seed-prod-catalog` trỏ đúng `docker-compose.prod.yml` (không thêm `migrate-refresh-prod`/`seed-prod-quotify`), cập nhật `docs/runbooks/deploy-rollback-restore.md` chèn bước 3.5/3.6 sau bước migrate, thêm test `test_quotify_seed_material_catalog_skips_suppliers_and_users`. Tiện tay sửa luôn 1 assertion test nợ cũ không liên quan (`test_quotify_seed_creates_catalogs_suppliers_and_users` hard-code 2 material type "Nguyên liệu"/"Vi lượng", chưa cập nhật theo 2 material type "Bao bì"/"Premix" thêm ở commit trước) để suite xanh trở lại. Kiểm chứng: `py_compile` pass, `ruff check` pass, `pytest tests/test_quotify_seed_service.py` 4 passed.

- Đã tạo runbook deploy production trên VPS ngày 10/08/2026 (`docs/runbooks/deploy-vps-production.md`) cho domain thật `quotify.honghafeed.com.vn`, build/deploy trực tiếp qua SSH (không CI/CD). Phát hiện và sửa 2 vấn đề hạ tầng chặn go-live thật: (1) `docker-compose.prod.yml` chưa có TLS — reverse-proxy chỉ nghe port 80, không phù hợp domain thật và mâu thuẫn với `AUTH_REFRESH_COOKIE_SECURE=true` (cookie secure cần HTTPS); (2) `CORS_ORIGINS` bị hard-code `localhost:${PROXY_HOST_PORT}` trong `environment:` của backend/worker, đè lên giá trị thật từ `.env` — domain thật sẽ luôn bị CORS chặn. Đã sửa: thêm service `certbot` (Let's Encrypt, tự renew loop 12h qua `--webroot`), `reverse-proxy` publish `80`/`443` (đổi từ biến `PROXY_HOST_PORT` sang `PROXY_HTTP_PORT`/`PROXY_HTTPS_PORT`), `docker/nginx/prod.conf` thêm ACME challenge location + redirect HTTP→HTTPS + server block 443 ssl cho domain `quotify.honghafeed.com.vn` (đã verify bằng `nginx -t` với chứng chỉ self-signed tạm — syntax hợp lệ), `CORS_ORIGINS` trong compose đổi thành đọc từ `.env` (`${CORS_ORIGINS}`) thay vì hard-code. Cập nhật `.env.production.example` (`DOMAIN_NAME`, `CERTBOT_EMAIL`, `CORS_ORIGINS` trỏ domain thật), `README.md`, `memory-bank/techContext.md` theo biến port mới. Runbook mới hướng dẫn đầy đủ: bootstrap cert lần đầu bằng `certbot certonly --standalone` (trước khi stack `up`, vì Nginx không start được nếu thiếu cert), migrate/seed lần đầu bằng `docker compose run` (chưa có container để `exec`), reload Nginx định kỳ sau renew (certbot không tự trigger reload cross-container), và các lần deploy sau dùng `make migrate-prod`/`seed-prod-auth`/`seed-prod-catalog` đã có. Kiểm chứng: `docker compose -f docker-compose.prod.yml config` pass, `bash scripts/compliance/check-production-readiness.sh` pass, `nginx -t` pass với upstream giả lập + cert tạm.

- Đã sửa bug import báo giá cũ (backfill import) ngày 10/08/2026: parser cột `received_date`/`delivery_month` trong `quote_backfill_import.py` giả định chuẩn ISO (`YYYY-MM-DD`/`YYYY-MM`), nhưng file lịch sử thực tế ghi theo `DD/MM/YYYY`/`MM/YYYY` — mọi dòng import thực tế đều bị từ chối. Đổi parser sang `datetime.strptime` với format thực tế, cập nhật sample row/template, thông báo lỗi, và toàn bộ fixture test theo chiều mới (trước đó test còn khẳng định `15/06/2026` là input sai). Xem `memory-bank/bugPatterns.md` mục 2026-08-10. Kiểm chứng: `ruff check` pass, `pytest tests` full 302 passed.

- Đã sửa bug import CSV báo sai header dù file đúng định dạng ngày ngày 10/08/2026: file thực tế người dùng upload có BOM UTF-8 bị mã hoá lại 2 lần (mojibake `"ï»¿"` thay vì byte `EF BB BF` thật), khiến `codecs.getincrementaldecoder("utf-8-sig")` không nhận diện được, làm `"ï»¿"` lẫn vào tên cột đầu (`"ï»¿supplier_name"`) và toàn bộ file bị báo sai header — reproduce chính xác bằng file mẫu người dùng gửi. Sửa `_iter_decoded_csv_lines` (`backend/app/worker.py`) bóc thêm tiền tố mojibake này ở dòng đầu, áp dụng chung cho cả backfill import báo giá cũ và import danh mục (cùng dùng 1 hàm decode). Đã restart container `worker` để áp dụng fix (worker không tự reload code như `backend`). Xem `memory-bank/bugPatterns.md` mục 2026-08-10 (BOM). Kiểm chứng: thêm `TestIterDecodedCsvLines` (3 case: BOM thật, BOM mojibake, không BOM), `ruff check` pass, `pytest tests` full 305 passed, reproduce bằng đúng file CSV người dùng gửi xác nhận parse đúng sau fix.

- Đã sửa bug backfill import báo "nhà cung cấp không tồn tại" dù NCC có sẵn ngày 12/08/2026: file thực tế test bằng NCC "ADM" (khớp `Supplier.code`, không khớp tuyệt đối `Supplier.name` đầy đủ "Archer Daniels Midland (ADM Asia)") bị từ chối vì matching cũ chỉ so bằng tuyệt đối với `Supplier.name`. Người dùng xác nhận file thực tế sẽ dùng tên viết tắt tương tự (ADM, BUNGE, Tân Long, CJ...) và yêu cầu tìm NCC khớp nhất, báo lỗi chi tiết khi không chắc. Thêm `_resolve_supplier_id` 2 tầng: khớp chính xác theo tên HOẶC mã trước, fallback sang containment (tên file là substring của tên/mã hệ thống) nếu không có khớp chính xác duy nhất; báo lỗi cụ thể nếu ambiguous hoặc không tìm thấy ở cả 2 tầng. Đã verify end-to-end bằng đúng file CSV thật người dùng gửi qua API thật (login → upload → check job) — import thành công, quote tạo đúng NCC "Archer Daniels Midland (ADM Asia)", giá quy đổi đúng — rồi dọn dữ liệu test khỏi DB dev. Xem `memory-bank/bugPatterns.md` mục 12/08/2026. Kiểm chứng: thêm `TestResolveSupplierId` (5 case), `ruff check` pass, `pytest tests` full 311 passed, đã restart container `worker`.

- Đã sửa bug UI panel trạng thái import ngày 12/08/2026: chữ "Hoàn tất" dính liền với số dòng thành công/lỗi, không có màu phân biệt thành công/thất bại. Điều tra bằng Playwright thật (không chỉ đọc code) phát hiện root cause thực chất không phải thiếu khoảng trắng: `_quotes-page.scss` nested toàn bộ class dùng trong `<Dialog>` (`__dialog`, `__form`, `__import-status`...) dưới `.quotes-page {}`, nhưng PrimeVue Dialog teleport nội dung ra `<body>` nên các nested selector này không bao giờ match (`getComputedStyle` xác nhận `display:block` thay vì `flex` đã khai báo). Đã chuyển các class đó thành top-level selector (giống cách Materials/Suppliers/MaterialTypes đã làm đúng), và thêm modifier `--success` (xanh) cho cả 4 trang import, đổi điều kiện `--failed` từ `status==='failed'` sang `failedRows>0` để phân biệt cả trường hợp thành công một phần. Đã verify bằng Playwright thật trong container frontend: chụp ảnh xác nhận cả 2 trạng thái xanh/đỏ hiển thị đúng, `display:flex` áp dụng đúng, dọn sạch file/job test khỏi DB dev. Xem `memory-bank/bugPatterns.md` mục 12/08/2026 (Dialog teleport). Kiểm chứng: `eslint` các file sửa sạch, `vite build` pass, `typecheck` không phát sinh lỗi mới (so baseline qua git stash), `test:unit` không phát sinh lỗi mới (5 failed pre-existing giống baseline).

- Đã đổi quyết định "không dò trùng lặp" của backfill import ngày 12/08/2026 sau khi người dùng test thực tế và tạo trùng dữ liệu 2 lần (import lại đúng file cũ). Xác nhận với người dùng: chỉ chặn khi trùng hoàn toàn dữ liệu dòng (NCC + ngày nhận + vật tư + giá gốc + kỳ giao hàng + tỷ giá + thuế + chi phí làm hàng), không chặn theo (NCC+ngày) đơn thuần vì NCC có thể báo giá 2 lần hợp lệ trong cùng ngày. Phát hiện trùng → báo lỗi rõ theo dòng trong file lỗi, không tạo báo giá mới cho cả nhóm. Implementation: phân giải NCC cho toàn bộ nhóm trước (in-memory, không tốn DB), rồi tải 1 query duy nhất toàn bộ `quote_lines` khớp (supplier_id, received_date) liên quan đến file đang import để so khớp O(1) mỗi dòng — giữ nguyên nguyên tắc hiệu năng không query theo từng dòng. Thêm `_load_existing_line_keys`/`_LineDedupKey` trong `quote_backfill_import.py`. Trong lúc verify bằng API thật phát hiện 2 báo giá trùng thật đang có trong DB dev (NCC ADM, note "Combo T8.9/ 10.11.12" — đúng dữ liệu người dùng dùng để test bug) — đã xác nhận với người dùng và xóa 1 bản trùng, giữ lại 1. Kiểm chứng: `ruff check` sạch, `pytest tests` full backend 313 passed (thêm 2 test dedup: chặn trùng hoàn toàn, cho phép giá khác cùng ngày), verify thật qua API xác nhận import lại đúng dữ liệu bị chặn với lỗi rõ ràng.

- Đã thêm 3 nhà cung cấp thật ngày 12/08/2026: ENERFO, GRAINLAND (quốc tế), ABC (trong nước), đều cung cấp Ngô hạt (CORN). Đã hỏi lại loại NCC trước khi tạo (không đoán). Tạo trước qua API thật (`POST /api/v1/suppliers`) để có ngay trong DB dev hiện tại, sau đó theo yêu cầu người dùng ("sau này tôi migrate rồi seed lại"), thêm luôn 3 `SupplierSeed` này vào cuối `SUPPLIER_SEEDS` trong `backend/app/quotify/seed_data.py` để không mất khi dev chạy lại `migrate-refresh`/`seed`. Lưu ý: production vẫn không chạy `seed_quotify.py` (quyết định cũ không đổi), nên 3 NCC thật này vẫn phải tạo thủ công trên production như các NCC thật khác — việc thêm vào seed chỉ để tiện dev. Kiểm chứng: `ruff check`/`py_compile` pass, `pytest tests` full backend 313 passed.

- Đã sửa bug tỷ giá "không phải là số hợp lệ" ngày 12/08/2026 khi import file thực tế có dấu phẩy ngăn hàng nghìn (ví dụ "26,466") — `Decimal("26,466")` raise `InvalidOperation`. Sửa `_parse_decimal` trong `quote_backfill_import.py` bỏ dấu phẩy trước khi parse, áp dụng cho mọi cột số. Đồng thời theo yêu cầu người dùng, thêm ràng buộc `exchange_rate` phải là số nguyên (`require_integer=True`, raise "Tỷ giá phải là số nguyên." nếu có phần lẻ), còn `price_original` giữ nguyên kiểu thập phân (giá USD có thể có cent). Trong lúc verify bằng API thật, phát hiện đã lỡ xóa nhầm 1 báo giá thật của người dùng (`b554fc24`, ADM/01-07-2026) bằng câu lệnh cleanup SQL quá rộng — đã báo lỗi minh bạch với người dùng và tạo lại đúng dữ liệu gốc qua API. Từ đó rút kinh nghiệm: chỉ dọn dữ liệu test bằng ID chính xác đã tạo ra, không dùng điều kiện rộng (theo thời gian/nhà cung cấp) trên DB dev đang được người dùng dùng thật đồng thời. Kiểm chứng: `ruff check` sạch, `pytest tests` full backend 317 passed, verify thật qua API cho cả 2 chiều (tỷ giá lẻ bị chặn, giá gốc lẻ được chấp nhận), dọn sạch bằng ID chính xác.

- Đã cải thiện tooltip chart "Giá theo kỳ hàng về" ngày 12/08/2026: trước đó lấy cứng 5 dòng đầu theo `received_date` tăng dần (ngẫu nhiên, không liên quan ý nghĩa), dù header ghi đúng tổng số (ví dụ "65 báo giá cho kỳ 12/2026"). Đổi sang chọn 5 báo giá tiêu biểu: giá thấp nhất + giá cao nhất (khớp 2 đường min/max đã có trên chart) + báo giá mới nhất + 2 báo giá gần giá trung bình nhất (ưu tiên NCC khác nhau để đa dạng), thêm dòng "... và N báo giá khác" khi còn dư. Đồng thời thêm click vào điểm trên chart để điều hướng sang `/quotes?deliveryMonth=YYYY-MM-DD` — đã thêm đọc `route.query.deliveryMonth` lúc mount ở `QuotesPage.vue` (trước đây trang này không hỗ trợ deep-link qua query string) để tự mở bộ lọc nâng cao và điền sẵn "Tháng giao hàng". File sửa: `frontend/src/composables/useDashboardPage.ts` (`pickRepresentativeTooltipPoints`, `onClick` trong `chartOptions`), `frontend/src/pages/QuotesPage.vue` (`onMounted` đọc `route.query`). Kiểm chứng bằng Playwright thật trong container: chụp ảnh tooltip xác nhận đúng nội dung (min/max/latest/gần-trung-bình + dòng "và N khác"), xác nhận click điều hướng đúng URL và trang `/quotes` tự mở bộ lọc + điền đúng tháng. `eslint`/`vite build`/`typecheck`/`test:unit` không phát sinh lỗi mới so với baseline.

- Đã gắn nhãn vai trò cho từng dòng trong tooltip chart "Giá theo kỳ hàng về" ngày 12/08/2026 theo phản hồi người dùng: dòng giá thấp nhất/cao nhất trước đó đã đúng là 2 báo giá được chọn (verify được bằng số liệu khớp với legend), nhưng không có nhãn nên người xem phải tự so số để nhận ra dòng nào là thấp nhất. Bước 1 thêm prefix text `[Giá thấp nhất]`/`[Giá cao nhất]`/... — sau đó người dùng yêu cầu đổi sang màu thay vì text. Đã chuyển tooltip từ canvas-drawn mặc định của Chart.js sang **external tooltip** (render bằng HTML/CSS thật, `plugins.tooltip.enabled: false` + `external(context)`) vì tooltip mặc định chỉ vẽ 1 màu chữ/dòng, không tô được ô màu riêng cho từng báo giá tiêu biểu. Mỗi dòng giờ có ô vuông màu khớp đúng màu series trên chart (tím = trung bình, xanh = thấp nhất, cam = cao nhất, xám = mới nhất/không có series tương ứng) — lấy màu qua `tooltip.labelColors[index].backgroundColor` cho 3 dòng gốc (lưu ý: dùng `backgroundColor` không phải `borderColor`, vì tất cả dataset dùng chung `pointBorderColor: panel` màu trắng làm viền điểm, nên `borderColor` luôn ra trắng — đây là bug thật đã sửa trong lúc verify). Thêm class `.quotify-chart-tooltip` + con trong `dashboard-page.scss`, đổi `.dashboard-page__chart-frame` từ `overflow:hidden` sang `visible` để tooltip HTML không bị cắt ở rìa khung. Kiểm chứng bằng Playwright thật (đọc `innerHTML` + chụp ảnh): màu từng dòng khớp chính xác `rgb` của dataset tương ứng. `eslint`/`vite build`/`typecheck`/`test:unit` không phát sinh lỗi mới.

- Đã sửa 2 vấn đề hiển thị tooltip chart "Giá theo kỳ hàng về" ngày 12/08/2026: (1) mỗi báo giá bị xuống 2 dòng do tooltip quá hẹp (`max-width: 24rem`) — tăng lên `width: max-content; max-width: min(38rem, 100vw - 2rem)` và thêm `white-space: nowrap` cho từng dòng; (2) tooltip ở gần 2 đầu chart bị lồi ra ngoài viewport và bị cắt mất nội dung do CSS căn giữa cố định (`transform: translate(-50%, -100%)` theo `caretX`) — chuyển sang tính `left`/`top` bằng JS sau khi đã đo kích thước thật của tooltip (`offsetWidth`/`offsetHeight`), kẹp trong phạm vi canvas (`offsetLeft` đến `offsetLeft + canvasWidth - tooltipWidth`) thay vì luôn căn giữa. Kiểm chứng bằng Playwright thật ở 3 vị trí (đầu, giữa, cuối chart, viewport 1600px): cả 3 đều hiển thị đầy đủ trong khung nhìn, mỗi báo giá 1 dòng. `eslint`/`vite build`/`typecheck`/`test:unit` không phát sinh lỗi mới.

- Đã sửa tiếp 2 vấn đề tooltip chart "Giá theo kỳ hàng về" ngày 12/08/2026 (phát sinh sau lần tăng độ rộng trước): (1) `white-space: nowrap` khiến chữ tràn ra ngoài khung tooltip khi dòng quá dài (không wrap nhưng cũng không bị cắt) — thêm `overflow: hidden` cho khung + `overflow:hidden; text-overflow:ellipsis; min-width:0` cho span chữ mỗi dòng làm lưới an toàn, đồng thời tăng `max-width` từ 38rem lên `min(50rem, 100vw - 2rem)` để dòng dài nhất thực tế (tên NCC dài + đủ 5 trường) hiển thị đủ trên desktop mà không cần cắt; (2) nền tooltip hard-code màu tối cố định (`rgba(15,23,42,0.92)`) gần trùng màu nền trang ở dark mode (`--app-surface-ground: #090d16`) khiến khó nhìn — đổi sang dùng token theo theme (`background: var(--app-surface-panel-alt); color: var(--app-text-primary); border: 1px solid var(--app-border-strong)`), tương phản tốt ở cả 2 theme. Kiểm chứng bằng Playwright thật: bật dark mode qua `.theme-switch`, xác nhận `document.documentElement.classList.contains('app-dark')`, chụp ảnh cả 2 theme xác nhận không còn tràn/che chữ. `eslint`/`vite build`/`typecheck`/`test:unit` không phát sinh lỗi mới.

- Đã triển khai V1 chart "So sánh giá nguyên liệu theo kỳ hàng về" ngày 12/08/2026 bằng TDD (skill `tdd`, chu trình RED→GREEN từng hành vi một, không viết hàng loạt test trước) theo đúng `docs/quotify/plan-multi-material-price-comparison-chart.md`. Không sửa backend (point_limit là cap toàn cục, không theo từng mặt hàng — nếu đổi filter sang `material_id IN (...)` sẽ có rủi ro 1 mặt hàng "ăn hết" quota của mặt hàng khác); thay vào đó gọi lại đúng endpoint hiện có N lần song song (tối đa 3 mặt hàng), merge ở frontend theo `deliveryMonth`. Thêm vào `useDashboardPage.ts`: `comparisonMaterialIds` (chọn tối đa 3, MultiSelect `selectionLimit`), `buildMaterialComparisonBuckets` (avg + số báo giá mỗi mặt hàng/kỳ, gap là `null` khi thiếu dữ liệu — không nối liền), `buildPriceDifferenceLines` (so trực tiếp nếu 2 mặt hàng, so với mặt hàng rẻ nhất nếu 3 mặt hàng), `comparisonChartData`/`comparisonChartOptions` (tái dùng nguyên hạ tầng external-tooltip vừa xây cho chart đơn — `getOrCreateChartTooltipElement`, kẹp vị trí, theme-aware — cho panel mới, màu theo MẶT HÀNG không tái dùng ý nghĩa màu vai trò của chart cũ). Thêm panel mới trên Dashboard, không sửa chart "Giá theo kỳ hàng về" hiện có. Trong lúc verify bằng Playwright thật phát hiện và sửa luôn 1 gap thật: `QuotesPage.vue` trước đó chỉ đọc `route.query.deliveryMonth` lúc mount (từ tính năng click-to-navigate của chart trước), chưa đọc `materialId` — khiến click từ chart so sánh chỉ lọc đúng kỳ mà bỏ qua mặt hàng; đã thêm đọc `materialId`. Cũng sửa 1 vấn đề UX nhỏ: thông báo trạng thái trống phân biệt rõ "chưa chọn đủ 2 mặt hàng" và "đã chọn đủ nhưng không có dữ liệu phù hợp" (trước đó luôn hiện thông báo đầu tiên, gây hiểu lầm). Kiểm chứng: 8 test mới trong `useDashboardPage.spec.ts` (chọn mặt hàng, fetch song song, merge/gap, callout chênh lệch 2 và 3 mặt hàng, cap tối đa 3, dataset chart) đều pass; phát hiện + sửa 1 regression thật trong `dashboard.page.spec.ts` (thiếu mock field mới); verify trực quan bằng Playwright thật với dữ liệu Ngô hạt/Khô đậu nành (đúng ví dụ trong kế hoạch) xác nhận tooltip, callout, và click điều hướng đều đúng. `eslint`/`vite build`/`typecheck` sạch, `test:unit` về đúng baseline (3 file/5 test fail, đều là nợ cũ không liên quan).

- Đã thêm dải giá thấp-cao (MIN-MAX) cho chart so sánh mặt hàng ngày 12/08/2026 theo phản hồi người dùng (chỉ có đường trung bình "ít thông tin quá"). Quyết định UX: mặc định hiện dải MIN-MAX cho TẤT CẢ mặt hàng đã chọn, mỗi mặt hàng có 1 checkbox riêng để untick ẩn dải của riêng nó (không ẩn đường trung bình). Bằng TDD: mở rộng `MaterialComparisonSeriesPoint`/`buildMaterialComparisonBuckets` thêm `minPrice`/`maxPrice` mỗi tháng; thêm `comparisonBandVisibility` (Record materialId→boolean, mặc định `true` khi mặt hàng được nạp lần đầu); `comparisonChartData` dựng dải bằng 2 dataset ẩn (max, min với `fill` trỏ vào index dataset max — cách chuẩn của Chart.js để tô vùng giữa 2 đường vì không có "band" dựng sẵn) chỉ khi `comparisonBandVisibility[materialId]` true. Ẩn 2 dataset helper này khỏi legend bằng `plugins.legend.labels.filter` (dựa vào hậu tố tên "nhất)") vì chúng không phải series có ý nghĩa để bật/tắt qua legend. Thêm checkbox theo từng mặt hàng trong `DashboardPage.vue` (`dashboard-page__comparison-band-toggle`, PrimeVue `Checkbox` binary). Kiểm chứng: thêm 2 test (mặc định hiện dải cho mặt hàng mới nạp; dataset dải xuất hiện/biến mất đúng khi toggle — verify thứ tự dataset chính xác khi 1 mặt hàng tắt dải còn mặt hàng kia bật), cập nhật 1 test cũ để cô lập đúng hành vi (tắt dải trước khi assert chỉ có đường trung bình). Sửa thêm 1 lỗi typing thật phát hiện lúc verify: interface `MaterialComparisonSeriesPoint` thiếu khai báo `minPrice`/`maxPrice` dù object literal đã có 2 trường này — an toàn nhờ vue-tsc bắt được khi dùng đường vòng khác để đọc field theo key động. Verify trực quan bằng Playwright thật: dải màu nhạt hiện đúng quanh đường trung bình mỗi mặt hàng, untick đúng 1 mặt hàng chỉ ẩn dải của mặt hàng đó, legend gọn còn 2 mục. `eslint`/`vite build`/`typecheck`/`test:unit` về đúng baseline.

- Đã sửa 2 vấn đề chart so sánh mặt hàng ngày 13/08/2026 theo phản hồi người dùng: (1) màu dải giá thấp-cao của Ngô hạt (`#2563eb`) khó nhìn ở dark mode vì gần tông nền tối — tăng alpha nền dải từ ~15% (`26`) lên ~30% (`4d`) riêng ở dark mode (`themeStore.mode === 'dark'`), giữ nguyên ở light mode vì đã rõ; (2) tooltip trước chỉ hiện giá trung bình, giờ hiện đủ `TB {avg} (Thấp {min} – Cao {max}) VNĐ/KG (n báo giá)` cho mỗi mặt hàng — thêm helper `formatNumber` (số không kèm đơn vị, để ghép chung 1 đơn vị VNĐ/KG cuối câu thay vì lặp lại 3 lần). Kiểm chứng bằng Playwright thật ở dark mode: dải xanh Ngô hạt rõ hơn hẳn, tooltip hiển thị đúng cả TB/Thấp/Cao. `eslint`/`vite build`/`typecheck`/`test:unit` về đúng baseline.

- Đã triển khai V1 chart thứ 3 "Diễn biến giá theo thời gian chào giá" ngày 13/08/2026 bằng TDD theo `docs/quotify/plan-price-history-by-received-date-chart.md`, sau khi user duyệt phương án thiết kế cho nhu cầu "xem giá lịch sử Ngô hạt/Khô đậu nành cho hàng về tháng 12/2026 dù báo giá đã có từ nhiều tháng trước". Đây là "twin" thời gian của chart so sánh trước: trục X đổi thành tháng NHẬN báo giá thay vì kỳ giao hàng, và kỳ giao hàng trở thành 1 filter cố định bắt buộc chọn (DatePicker `view="month"` riêng) thay vì trục biến thiên. Không sửa backend — endpoint `GET /dashboard/quotify/price-trends` đã hỗ trợ sẵn `delivery_month` + `material_id` cùng lúc, vẫn gọi N lần song song (tối đa 3 mặt hàng) như chart trước. Refactor DRY dưới GREEN: tổng quát hóa `buildMaterialComparisonBuckets` thành `buildGroupedComparisonBuckets(materialResults, getGroupKey)` (nhận hàm trích khóa nhóm, dùng chung cho cả nhóm-theo-kỳ-giao-hàng và nhóm-theo-tháng-nhận-báo-giá), và tách `comparisonChartData`/`comparisonChartOptions` thành `buildComparisonChartData`/`buildComparisonChartOptions` dùng chung cho cả 2 chart (đổi field `MaterialComparisonBucket.deliveryMonth` → `groupKey` cho trung lập ý nghĩa). Panel mới có state riêng `historyDeliveryMonth`/`historyMaterialIds`/`historyBuckets`/`historyBandVisibility`/`historyTrendResults`/`historyChartData`/`historyChartOptions`/`loadPriceHistory`, độc lập hoàn toàn với bộ lọc chung Dashboard và 2 panel còn lại. Trong lúc verify bằng Playwright thật phát hiện và sửa đúng gap đã lường trước trong kế hoạch: `QuotesPage.vue` chưa đọc `receivedDateStart`/`receivedDateEnd` từ query string (chỉ có `materialId`/`deliveryMonth` từ chart trước) — đã thêm đọc 2 field này lúc `onMounted`, mở bộ lọc nâng cao nếu có giá trị hợp lệ. Kiểm chứng: 5 test mới trong `useDashboardPage.spec.ts` (gating bắt buộc chọn kỳ giao hàng, fetch song song đúng `delivery_month` cố định, merge theo tháng nhận báo giá có gap, tái dùng callout chênh lệch, dataset chart) — tổng 19/19 test pass; cập nhật mock `dashboard.page.spec.ts` thêm các field history mới. Verify trực quan bằng Playwright thật với dữ liệu thật: Ngô hạt (nhận báo giá 07-08/2026) và Khô dầu đậu nành (nhận báo giá 01-08/2026) cho kỳ giao hàng 12/2026 — xác nhận đúng gap hiển thị đúng như dữ liệu DB thật (không nội suy), dải MIN-MAX + toggle hoạt động, và click-through điều hướng đúng `/quotes?materialId=...&deliveryMonth=2026-12-01&receivedDateStart=2026-07-01&receivedDateEnd=2026-07-31`. Trong lúc viết Playwright cũng phát hiện 1 bẫy test (không phải bug sản phẩm): `getByPlaceholder('Chọn vật tư')` khớp vào input ẩn dùng cho accessibility (`data-pc-section="hiddeninput"`) của PrimeVue MultiSelect thay vì phần tử hiển thị — phải click vào `.p-multiselect` thay vì input ẩn. `eslint`/`vite build`/`typecheck`/`test:unit` (98 test, 3 file/5 test fail là nợ cũ không liên quan: `quotify-settings.mappers.spec.ts`, `useQuotifySettingsPage.spec.ts`, `audit-logs.page.spec.ts`) về đúng baseline.

- Đã sửa lỗi tooltip không hiện khi hover chart "Diễn biến giá theo thời gian chào giá" ngày 13/08/2026 (user báo lại ngay sau khi tôi báo tính năng đã xong): nguyên nhân là lúc tổng quát hóa `comparisonChartOptions`/`historyChartOptions` thành `buildComparisonChartOptions(buckets, trendResults, buildNavigationQuery)` dùng chung cho 2 chart, `onClick` đã được tham số hóa đúng (`buckets[element.index]`) nhưng phần `external(context)` của tooltip bị bỏ sót, vẫn đọc cứng `comparisonBuckets.value` (biến của chart so sánh cũ) thay vì tham số `buckets` đang có sẵn trong scope. Với chart so sánh cũ trùng hợp vẫn đúng (buckets của nó chính là `comparisonBuckets.value`), nhưng với chart mới `historyBuckets` không bao giờ được đọc → tooltip luôn nhận `bucket` rỗng/không khớp và tự ẩn (`opacity: 0`) — không có lỗi console, không có test nào bắt được vì tooltip Chart.js chưa từng được unit test trong dự án này. Đã sửa 1 dòng (`buckets[tooltip.dataPoints[0]?.dataIndex]` thay vì `comparisonBuckets.value[...]`), thêm 1 test mới gọi thẳng `historyChartOptions.value.plugins.tooltip.external(fakeContext)` với canvas thật (không cần context 2D) để chặn tái phát — xác nhận test này FAIL đúng như mong đợi khi revert lại lỗi, PASS sau khi sửa. Verify lại bằng Playwright thật: tooltip hiện đúng nội dung cho cả 2 mặt hàng, TB/Thấp/Cao, số báo giá, dòng chênh lệch. Bài học: khi tổng quát hóa 1 hàm dùng chung nhiều closure biến ẩn (`comparisonBuckets.value`, `comparisonTrendResults.value`, ...) thành tham số, phải rà **tất cả** các nơi đọc biến cũ trong thân hàm (kể cả nhánh phụ như tooltip `external`), không chỉ nhánh chính (`onClick`) — nên grep lại toàn bộ tên biến cũ sau khi tham số hóa để chắc chắn không còn sót.

- Đã thêm cột "Tỷ giá" vào bảng `/quotes` (`Bảng báo giá`) ngày 13/08/2026 theo yêu cầu người dùng. `exchangeRate` đã có sẵn trên `QuoteFlattenedDomain`/mapper, chỉ cần thêm 1 `Column field="exchange_rate"` trong `QuotesPage.vue` (đặt giữa "Giá gốc" và "Kỳ giao hàng" — đúng vị trí logic vì tỷ giá là yếu tố quy đổi giá gốc) + hàm `formatExchangeRate` (2 chữ số thập phân, không đơn vị, giống style `formatMoney` ở `QuoteDetailPage.vue`), và thêm dòng tương ứng vào card mobile. Không sortable (giống cột thuế/chi phí làm hàng, tránh giả định backend hỗ trợ sort field này). Không sửa backend/mapper vì field đã tồn tại sẵn. Kiểm chứng bằng Playwright thật: cột hiện đúng giá trị tỷ giá cho các dòng USD/MT (vd `26,480.00`), để trống đúng cho dòng VNĐ/KG (không cần quy đổi ngoại tệ nên đúng nghĩa là null). `eslint`/`typecheck`/`test:unit`/`vite build` về đúng baseline.

- Đã cho phép nhập tỷ giá tham khảo (lịch sử) cho dòng VND/KG khi import backfill báo giá ngày 13/08/2026, theo phản hồi người dùng: trước đó `quote_backfill_import.py` chặn cứng CẢ 3 field (tỷ giá, thuế nhập khẩu, chi phí làm hàng) cho dòng VND/KG với lỗi "không được nhập tỷ giá, thuế nhập khẩu hoặc chi phí làm hàng"; người dùng cần nhập được tỷ giá cho dòng VND/KG vì nhân viên thu mua cần tra cứu lại bối cảnh tỷ giá tại thời điểm báo giá để hiểu vì sao giá cao/thấp lúc đó — dù VND/KG không cần quy đổi giá nên tỷ giá này thuần túy tham khảo. Sửa 2 lớp: (1) `quote_backfill_import.py` — validation dòng VND/KG giờ chỉ chặn thuế nhập khẩu/chi phí làm hàng, không còn chặn tỷ giá; (2) `quote_pricing.py` `resolve_pricing_provenance` (dùng chung bởi MỌI luồng tạo báo giá, không chỉ backfill) — nhánh VND/KG giờ nhận `manual_rate` tùy chọn, lưu với `exchange_rate_source_mode="manual_reference"` (giá trị mới, thêm vào comment doc ở `QuoteLine` model), KHÔNG ảnh hưởng `price_converted_vnd_per_kg` (vẫn luôn = `price_original`). Không cần sửa UI nhập báo giá thường (`QuoteEditorPage.vue`) vì `ExchangeRateField` chỉ hiển thị cho dòng USD/MT (`v-if="line.currency==='USD' && line.unit==='MT'"`) — thay đổi này chỉ có tác dụng thực tế qua cột CSV backfill import. Cột "Tỷ giá" mới thêm ở bảng `/quotes` (xem entry trước) tự động hiển thị đúng giá trị này cho dòng VND/KG mà không cần sửa gì thêm vì mapper không lọc theo currency. Kiểm chứng: cập nhật test parser (`test_vnd_kg_row_rejects_historical_values` tách thành `test_vnd_kg_row_rejects_tax_and_processing_cost` chỉ còn 2 field + thêm `test_vnd_kg_row_allows_optional_historical_exchange_rate`), thêm 1 test tích hợp đầy đủ qua `QuoteService.create_quote` thật (`test_create_quote_vnd_kg_allows_optional_historical_exchange_rate_for_reference`) xác nhận `exchange_rate`/`exchange_rate_source_mode`/`exchange_rate_manual_reason` lưu đúng và `price_converted_vnd_per_kg` không đổi. `pytest` backend đầy đủ 317 passed 1 skipped; `ruff`/`mypy` không phát sinh nợ mới so với baseline.

- Đã thêm 18 nhà cung cấp nội địa mới vào `SUPPLIER_SEEDS` (`backend/app/quotify/seed_data.py`) ngày 13/08/2026 theo yêu cầu người dùng: Anh Khoa, APEX, B&T Việt Nam, Bảo Lâm, Cao Thăng, COFCO, HANOFEED, Hà Thị, Minh Hiền, Ngọc Long, Nhất Thành, Quang Dũng, TACN Hà Nội, Thuận An, Thịnh Vượng, Tân Long, Tân Long (Cao Thăng), Việt Anh — tất cả `supplier_type="domestic"`, gắn 2 vật tư `CORN`+`SOYBEAN_MEAL`. Lưu ý quan trọng: "Tân Long" và "Tân Long (Cao Thăng)" là 2 nhà cung cấp MỚI, KHÁC với nhà cung cấp "Tập đoàn Tân Long (Tan Long Group)" (code `TAN_LONG`) đã có sẵn trong DB thật — đã chọn code riêng `TAN_LONG_TRADING`/`TAN_LONG_CAO_THANG` để tránh trùng, và ghi rõ trong note để không nhầm lẫn khi tra cứu. Đã kiểm tra DB thật (`select code,name from suppliers`) trước khi thêm để xác nhận không có tên nào trong danh sách 18 NCC mới đã tồn tại. Seeder idempotent theo `code` (không phải theo `name`) — đã chạy `scripts/seed_quotify.py` 2 lần trên DB dev thật để xác nhận: lần 1 tạo đúng 18 NCC mới + 36 cặp supplier-material, lần 2 không tạo trùng. `ruff`/`mypy`/`pytest` (317 passed, 1 skipped) không phát sinh nợ mới; các test seed cũ tự thích ứng vì dùng `len(SUPPLIER_SEEDS)` động, không hard-code số lượng.

- Đã sửa bug thật "chart Dashboard mặc định hiện dữ liệu cũ từ 2023 dù hôm nay đã là 8/2026" ngày 13/08/2026 (người dùng phát hiện qua ảnh chụp chart "Giá theo kỳ hàng về"). Nguyên nhân: `_get_points` trong `quotify_dashboard_service.py` sắp xếp `received_date.asc()` rồi `LIMIT 500` (mặc định, khi Dashboard không có bộ lọc "Từ/Đến ngày nhận" nào — đúng là trạng thái mặc định lúc load trang). DB thật có **16,196 dòng báo giá** trải từ 2023-10-10 đến 2026-08-04 — sắp tăng dần + limit nghĩa là luôn lấy đúng 500 dòng CŨ NHẤT, không bao giờ chạm tới dữ liệu gần hiện tại trừ khi người dùng tự lọc thu hẹp khoảng ngày nhận. Sửa: đổi `order_by` thành `received_date.desc()` (ưu tiên 500 dòng MỚI NHẤT), sau đó `reversed()` lại danh sách kết quả trong Python để trả về đúng thứ tự tăng dần (cũ→mới) như hợp đồng cũ, không phá giả định của tầng build chart phía trên. Thêm test `test_get_price_trends_prefers_most_recent_points_when_over_the_limit` xác nhận: (1) SQL có `received_date DESC`, (2) kết quả trả ra vẫn đúng thứ tự tăng dần. Verify trực tiếp qua API thật (`curl .../price-trends` với admin token): trước sửa dữ liệu sẽ bắt đầu từ 2023-10; sau sửa, `received_date` range của 500 điểm trả về là `2026-07-14 .. 2026-08-04` — đúng như mong đợi. `pytest` 318 passed 1 skipped, `ruff`/`mypy` sạch.

## Open

- Finish unrestricted verification for Phase 5 dependency audits and host-side performance smoke outside the current sandbox DNS/socket limits
- Verify Phase 6 observability stack `up` flow and execute a real restore drill in an isolated runtime environment
- Optionally install Bun and run the upstream `agent-memory` CLI locally
- Điều tra và sửa nợ kỹ thuật frontend đang chặn `make docker-test-frontend`: lint/typecheck lỗi ở `quotes.api.ts`, `useQuoteDetail.ts`, `useQuoteEditor.ts`, `QuoteDetailPage.vue`, `QuoteEditorPage.vue`, `QuotesPage.vue`, `exchange-rates.mappers.ts`, `quotes.mappers.ts`; build còn warning CSS cũ về `:deep(...)` trong `backups-page__actions`.
